# D03 挂号域接口实测记录（curl 等价 Apifox · 成员A · 2026-09-11）

> 环境：本地 MySQL 8（init.sql 临时副本重建，种子 11 科室/8 医生/57 排班）+ his-api 8080
> 分支：feature/a-D03-registration @ 85d041c · 测试账号 d03tester（userId=1），就诊人 id 1=张小满 / 2=张奶奶

## 一、正常路径

| 步骤 | 请求 | 响应要点 | 结果 |
| --- | --- | --- | --- |
| S1 注册 | POST /api/auth/register | code=200 注册成功 | ✅ |
| S2 登录 | POST /api/auth/login | code=200，Bearer token | ✅ |
| S7 加就诊人 | POST /api/members ×2 | code=200；列表回显中文正常 | ✅ |
| S8 挂号（scheduleId=1 主治·刘洋） | POST /api/registrations | `{orderNo:"20260911205651472203164"(23位), registerFee:15.00}`，message「挂号成功」 | ✅ 费用=职称映射正确 |
| 扣号核验 | SQL 查 schedule#1 | left_quota 15→14 | ✅ 原子扣号生效 |
| S9 我的挂号 | GET /api/registrations/my | 9 字段全对：memberName 联查「张小满」、deptName 快照「儿科」、doctorName 快照「刘洋」、workDate 2026-09-11、timeSegment 上午、registerFee 15.00、status 待就诊、createTime "2026-09-11 20:56:51" | ✅ 倒序+格式契约 |
| S10 取消 | POST /api/registrations/{orderNo}/cancel | code=200 取消成功，data=null | ✅ |
| 回补核验 | SQL 查 schedule#1 + 订单状态 | left_quota 14→15；status=已取消 | ✅ 直达回补生效 |

## 二、错误分支

| 用例 | 请求 | 响应 | 结果 |
| --- | --- | --- | --- |
| 3004 | 已取消订单再次 cancel | code=3004 当前状态不可取消 | ✅ |
| 3006 | memberId=999（不属于本人） | code=3006 就诊人不存在或不属于当前用户 | ✅ |
| 3005 | scheduleId=99999 | code=3005 排班不存在 | ✅ |
| 3006(S10) | orderNo 不存在 | code=3006 挂号订单不存在或不属于当前用户 | ✅ |
| 401 | 无 Token POST /registrations | HTTP 401 | ✅ |

## 三、DoD#2 防超卖实证

- SQL：`UPDATE schedule SET left_quota=1 WHERE id=2;`（副主任·陈晓云）
- 第 1 次提交 → `200 挂号成功 registerFee:20.00`，库内 1→0
- 第 2 次提交 → `3001 该时段号源已约满`，库内仍 0
- **结论：条件更新 `WHERE left_quota>0` 数据库层兜底成立，无超卖**（测后经取消回补+恢复种子值）

## 四、费用防篡改

- 请求体塞 `"registerFee":999` → 响应仍 `registerFee:30.00`（主任医师映射，schedule#57·doctorId=3）
- **结论：请求体无费用字段+后端重算落库快照，前端传什么都不影响**（6.5-⑥）

## 五、放行清单补录验证

- 无 Token `GET /api/quotas` → HTTP 404（非 401，已过拦截器；控制器待 B 实现）
- 无 Token `GET /api/quotas/days?deptId=1` → HTTP 404（同上）
- **结论：V1.9 两个公开查询已放行，且不影响同路径写方法的拦截**

## 六、测试数据清理

两张测试单均经 API 取消回补，schedule#2 恢复种子值 20；appointment_record 留 3 条「已取消」订单（订单永久保留口径）。
