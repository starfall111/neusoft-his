# Web 管理端 · 页面设计稿（S11/S12 + 迭代 M5/M6，Element Plus）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.2（2026-09-09：迭代新增 M5 登录页、M6 统计大屏；M4 的 401 规则由"提示页"改为"跳 M5 登录页"；M5/M6 接口为**待对齐**，见文末「M5/M6 契约待对齐记录」） |
| 配套规范 | doc/ui/UI统一规范.md 第 3 节（Element Plus 默认令牌，不自造色值） |
| 数据契约 | db/dto-contract.md、db/mock-data.md（M5 复用 /auth/login；M6 统计接口组**待对齐**） |
| 页面清单 | M1 布局框架 · M2 科室管理 · M3 医生管理 · M4 全局规则（401/网络/状态矩阵）· M5 登录页 · M6 统计大屏 |

> 技术基线：Vue3 + Element Plus + Vue Router + Pinia + axios；布局即"侧边菜单 + 顶栏 + 内容区"的常规后台框架，不追求视觉创新，追求**状态齐全、与契约零漂移**。V1.2 起管理端自带登录页（M5），统计大屏（M6）为迭代新增演示页。

---

## M1 布局框架

```
┌──────────┬──────────────────────────────────────┐
│          │  东软云医院管理端                       │  顶栏 56px 白底
│  侧边栏   ├──────────────────────────────────────┤
│  220px   │                                      │
│ ┌──────┐ │   内容区 padding:16px                 │
│ │ 科室管理│ │   ┌────────────────────────────┐   │
│ │ 医生管理│ │   │  页头：页面标题     [+主操作]  │   │
│ └──────┘ │   │  表格卡片（白底圆角）          │   │
│          │   │  …                          │   │
│          │   │              分页 ▸ 右下角     │   │
│          │   └────────────────────────────┘   │
└──────────┴──────────────────────────────────────┘
```

| 元素 | 规范 |
| --- | --- |
| 侧边栏 | `el-menu` 竖向，浅色底，三项（V1.2）：科室管理（/admin/departments 页）、医生管理（/admin/doctors 页）、统计大屏（/admin/dashboard 页，V1.2 新增）；选中项 primary 高亮 |
| 顶栏 | 标题左对齐；右侧预留（登录态展示为扩展项，本期仅统计大屏页放当前日期） |
| 内容区 | 每页结构固定：**页头（标题+主操作按钮）→ el-card 表格 → el-pagination 右下**（M6 大屏除外，见 M6 节） |
| 路由 | /login（M5，免登录）、/departments、/doctors、/dashboard；默认重定向 /departments；未登录访问受控页 → 跳 /login（V1.2 起，原 /401 提示页方案废弃） |

---

## M2 科室管理页（S11）

**数据来源**：`GET / POST / PUT / DELETE /admin/departments`；列表 → `PageDTO<DepartmentRespDto>`（V1.1 起含 parentId）；保存 → `DepartmentSaveReqDto{deptName, parentId, deptIntro}`（V1.1：改传父分类 id，不再传分类文本）

```
┌──────────────────────────────────────────────┐
│ 科室管理                        [+ 新增科室]   │  页头：标题 + 主按钮
│ ┌──────────────────────────────────────────┐ │
│ │ ID │ 科室名称  │ 科室分类 │ 科室简介  │ 操作   │ │
│ │ 1  │ 心血管内科 │ 内科    │ 高血压、… │ 编辑 删除│ │  操作列固定右侧
│ │ 2  │ 消化内科   │ 内科    │ 胃肠疾病、…│ 编辑 删除│ │  简介 show-overflow-tooltip
│ │ …  │ 儿科      │ 妇儿    │ 儿童呼吸道…│ 编辑 删除│ │
│ └──────────────────────────────────────────┘ │
│                     ◀ 1 2 3 ▶   共 4 条  10条/页│  分页右下
└──────────────────────────────────────────────┘

[+ 新增科室] / [编辑] → Dialog（宽 520px）：
┌──────────────────────────────┐
│  新增科室                  ✕  │
│  科室名称*  [________________] │  ≤100，必填
│  父分类*    [内科        ▼]   │  下拉=父分类节点（GET /admin/departments 过滤 parentId 为空；七类预置）
│  科室简介   [____________]    │  textarea ≤500，选填
│              [取消]  [保 存]   │  保存带 loading
└──────────────────────────────┘
[删除] → ElMessageBox.confirm（type: warning）：
「删除后前台将不可见，是否继续？」[取消] [删除(danger)]
```

| 元素 | 字段/行为 | 说明 |
| --- | --- | --- |
| 表格列 | `id / deptName / deptCategory / deptIntro` | 分类列为父分类名称冗余（后端按 parentId 维护，管理端只读展示；可加 el-tag 装饰） |
| 分页 | `pageNum / pageSize` ↔ el-pagination；响应 `PageDTO{total, pageNum, pageSize, records}` | 删除当前页最后一条后回退一页 |
| 保存 | POST / PUT 同一 Dialog（编辑时回填 + 隐藏 id；提交 deptName/parentId/deptIntro） | 成功 → `ElMessage.success` + 关弹窗 + **刷新当前页**（后端写时删缓存，App 端立即可见——演示点） |
| 删除 | DELETE /{id}（逻辑删除 delmark=1） | 删除**父分类节点**时若其下仍有子科室 → 400「该分类下存在科室，无法删除」（后端校验，前端展示 message）；删除子科室不受限 |
| 错误 | 400（校验）/ 其他 → `ElMessage.error(message)` | |

**状态**：v-loading / el-empty（无数据）/ 错误 Message / 正常。

---

## M3 医生管理页（S12）

**数据来源**：`GET / POST / PUT / DELETE /admin/doctors`；列表 → `PageDTO<DoctorRespDto>`；保存 → `DoctorSaveReqDto{doctorName, deptId, title, skill}`

```
┌────────────────────────────────────────────────────┐
│ 医生管理    科室筛选[全部科室 ▼]（可选，待对齐） [+ 新增医生]│
│ ┌────────────────────────────────────────────────┐ │
│ │ ID │ 医生姓名 │ 所属科室  │ 职称     │ 擅长领域 │ 操作  │ │
│ │ 1  │ 周建国   │ 心血管内科 │ 主任医师 │ 冠心病…  │ 编辑 删除│ │  职称中文直传
│ │ 5  │ 李强     │ 普通外科   │ 副主任医师│ 腹腔镜…  │ 编辑 删除│ │
│ └────────────────────────────────────────────────┘ │
│                          ◀ 1 ▶   共 8 条             │
└────────────────────────────────────────────────────┘

[+ 新增医生] / [编辑] → Dialog（宽 520px）：
┌──────────────────────────────┐
│  新增医生                  ✕  │
│  医生姓名*  [________________] │  ≤50
│  所属科室*  [心血管内科   ▼]   │  下拉=GET /admin/departments 过滤 parentId 非空（仅子科室）；必选
│  职称*      [主任医师     ▼]   │  下拉三档中文：主任医师/副主任医师/主治医师
│  擅长领域   [____________]    │  textarea ≤500
│              [取消]  [保 存]   │
└──────────────────────────────┘
│  ℹ️ 挂号费由系统按职称自动定价（主任30/副主任20/主治15），无需填写 │  Dialog 底部灰色说明
```

| 元素 | 字段/行为 | 说明 |
| --- | --- | --- |
| 表格列 | `id / doctorName / deptName / title / skill` | 职称列可直接 el-tag 装饰（三档同色或分级着色，仅装饰） |
| 科室下拉 | 打开 Dialog 时拉取科室全量（复用科室接口，不分页全量参数【待对齐：pageSize 上限】），**过滤 parentId 非空的子科室**（V1.1：父分类节点不挂医生） | deptId 为空提交 → 前端拦截；4001 兜底 Message |
| 职称下拉 | 三个固定中文选项，与 CHECK 约束取值一致；**表单无挂号费字段**（费用=职称映射，拆解文档 6.5-⑥） | |
| 删除 | DELETE /{id}（逻辑删除 delmark=1，且后端同事务逻辑删其未来排班——前端无感，只需成功提示+刷新） | |
| 编辑 | 回填四字段；doctorName 允许改名（V2.1 起取消回补按 schedule_id，不依赖姓名） | |

**状态**：v-loading / el-empty / 4001 等错误 Message / 正常。

---

## M4 全局规则（管理端）

| 项 | 规范 |
| --- | --- |
| axios 封装 | baseURL `http://localhost:8080/api`；请求拦截器注入 `Authorization: Bearer {token}`（token 来自 M5 登录，存 localStorage）；响应拦截器：`code===200` 透传 data，否则 `ElMessage.error(message)` 并 reject；HTTP 401 → `ElMessage.warning('登录已失效')` + 清 token + 跳 /login（V1.2 起） |
| 路由守卫 | 除 /login 外全部页面为受控页：无 token → 重定向 /login（与后端拦截器 /admin 前缀口径一致） |
| 网络异常 | 请求 reject 且无 response → `ElMessage.error('网络连接异常，请检查网络')` |
| 类型定义 | `src/api/` 集中定义：接口函数 + TS 类型（从 dto-contract.md 逐字段抄写；M6 统计接口待对齐期间单独放 `src/api/stats.ts` 并注释待对齐标记）；组件禁止手写 URL/字段（规则八） |
| 加载 | 表格 v-loading；Dialog 保存按钮 loading；禁止双层 loading |
| 表单校验 | rules 与 dto-contract 校验列一致（必填/长度/枚举下拉）；后端 400 时以 message 为准展示 |
| 空态 | 表格自带 el-empty；筛选无结果同空态；图表数据为空时整卡显示 el-empty |
| 刷新纪律 | 增/删/改成功后**刷新当前页**（保持 pageNum；删空当前页则 pageNum-1） |

---

## M5 登录页（V1.2 迭代新增）

**数据来源**：`POST /auth/login`（复用患者端登录接口，**管理端鉴权口径待对齐**，见文末待对齐记录 #12）

```
┌──────────────────────────────────────────────┐
│            （页面底 #F0F2F5 / ep-page-bg）      │
│                                              │
│         ┌──────────────────────┐             │
│         │      [Logo 蓝块]      │  48×48 primary 圆角
│         │   东软云医院管理端      │  20px 600
│         │  挂号预约系统·管理端登录 │  13px secondary
│         │                      │             │
│         │  [👤 请输入用户名     ] │  40h 输入框带左侧图标
│         │  [🔒 请输入密码    👁 ] │  密码框+明文切换
│         │  ⚠ 用户名或密码错误    │  仅错误态显示（danger 红字+两输入框红描边）
│         │  [    登  录     ]    │  主按钮通栏 40h；两框任一为空→禁用
│         │   仅限管理人员使用     │  12px tertiary
│         └──────────────────────┘             │
│            （卡片 400px 宽，水平居中）           │
└──────────────────────────────────────────────┘
```

| 元素 | 字段/行为 | 说明 |
| --- | --- | --- |
| 表单 | 用户名/密码两输入框（el-input，带 prefix 图标、密码 show-password） | 前端只做非空校验，格式校验交后端 |
| 提交 | `POST /auth/login`，body=`LoginReqDto{username,password}`；提交中按钮 loading 且禁点 | 成功：token 存 localStorage → 跳 /departments |
| 错误态 | 1002 → 输入框红描边 + 框下红字「用户名或密码错误」（el-form error 行内提示风格，非 Message 弹出） | 网络异常 → `ElMessage.error` 固定文案 |
| 路由 | /login 免登录；已登录访问 /login → 重定向 /departments | |

**状态**：正常 / 提交中 / 业务错误（红框+红字）/ 网络错误 / 两框空时按钮禁用。设计稿见「M5 管理端登录·正常态.png / ·错误态.png」。

---

## M6 统计大屏页（V1.2 迭代新增）

**数据来源**：统计接口组**待对齐**（见文末待对齐记录 #13）；落地前前端用本节示例数值 mock。

**路由**：/admin/dashboard（侧边栏第三项「统计大屏」，选中 primary 高亮）；整体沿用 M1 框架（侧边栏 220 + 顶栏 56 + 内容区 padding 16），内容区为卡片网格而非"页头+表格"。

```
┌────────┬───────────────────────────────────────────────────────┐
│ 侧边栏  │ 顶栏：统计大屏                        2026-09-08 周二 14:32 │
│ 科室管理 ├───────────────────────────────────────────────────────┤
│ 医生管理│ ┌今日挂号┐┌今日取消┐┌注册用户┐┌今日收入┐   KPI 四卡（图标底四色）   │
│ 统计大屏│ │128 ↑12%││9  ↓3% ││1,024↑5%││¥2,860↑8%│   环比↑success ↓danger   │
│(选中)  │ └────────┘└────────┘└────────┘└────────┘                       │
│        │ ┌─────────────────────────┐┌───────────────┐                  │
│        │ │ 近 7 天挂号趋势（折线）      ││ 今日时段分布      │                  │
│        │ │  Y轴80–140·日期09-02~08   ││ 上午96·下午32   │                  │
│        │ │  面积填充+7数据点          ││ 左大数字+右双柱  │                  │
│        │ └─────────────────────────┘└───────────────┘                  │
│        │ ┌科室挂号量┐┌医生TOP5 ┐┌职称占比+最新动态┐                       │
│        │ │ 柱状×4   ││ 条形×5   ││ 进度条×3+列表×4 │                       │
│        │ └─────────┘└─────────┘└───────────────┘                       │
└────────┴───────────────────────────────────────────────────────┘
```

**七个图表卡与示例数据（mock 期取值，联调后以接口为准）**：

| 卡片 | 图表类型 | 示例数据 | 用色（EP 令牌） |
| --- | --- | --- | --- |
| KPI 行 ×4 | 图标+大数字+环比 | 今日挂号 128（↑12%）/ 今日取消 9（↓3%）/ 注册用户 1,024（↑5%）/ 今日收入 ¥2,860（↑8.4%） | 图标底 primary/danger/success/warning ×对应 light |
| 近 7 天挂号趋势 | 折线+面积填充+数据点 | 86 / 102 / 78 / 120 / 95 / 140 / 128（09-02~09-08，Y 轴 80–140） | 线 primary 2.5px，面积 primary 10% 透明，网格 border-light |
| 今日时段分布 | 左大数字 + 右双柱 | 上午 96（75%）/ 下午 32（25%） | 上午 primary / 下午 success |
| 科室挂号量 | 柱状 ×4 | 心血管 86 / 儿科 62 / 消化 54 / 普外 38 | 柱 primary，数值 primary 文本 |
| 医生挂号量 TOP5 | 横向条形 ×5 | 周建国 45 / 吴敏 32 / 陈晓云 28 / 李强 21 / 王秀英 17 | 条 primary，槽 fill-light，前三名次 primary |
| 职称挂号占比 | 进度条 ×3 | 主任医师 58 单·46% / 副主任医师 42 单·33% / 主治医师 26 单·21% | primary / success / warning |
| 最新挂号动态 | 列表 ×4 | 时间 + 就诊人·科室 医生 + 挂号费（¥30/¥20/¥15） | 状态点 success，金额 primary |

**实现约定**：

1. 图表库**待对齐**（记录 #14，倾向 ECharts 5 + vue-echarts；零基础备选纯 CSS 柱状/条形 + SVG 折线）；
2. 统计口径与业务口径一致：挂号量 = 近 7 天**有效**挂号单（不含已取消）；收入 = 挂号费快照 register_fee 求和；职称占比用 title 中文直传；
3. 示例数值与种子数据医生/科室/职称三档一致（医生名取自 init.sql 种子），联调前 mock 不偏离本表；
4. **状态**：每卡 v-loading；接口失败 → 卡内 el-empty + `ElMessage.error`；空数据 → el-empty（大屏整体不因单卡失败白屏）。

设计稿见「M6 统计大屏.png」。

---

## 管理端验收自查（对照 S11/S12 验收标准 + V1.2 迭代）

| 页 | 必验项 |
| --- | --- |
| M2 科室 | 表格四列 + 分页；新增/编辑 Dialog 三字段（父分类动态下拉，V1.1）；删除父分类被 400 拦截演示；删除二次确认；**改科室后 App 端立即可见**（缓存联动，与 D 三方验证） |
| M3 医生 | 表格五列（科室名/职称中文）；科室下拉必选；职称三档中文直传；无费用字段（Dialog 底部说明）；删除联动排班逻辑删 |
| M4 全局 | 五态演示齐全（loading/空/错误/正常/禁用）；401 清 token 跳 /login；无 token 访问受控页被路由守卫拦截；所有字段与 dto-contract.md 零漂移 |
| M5 登录 | 两框空→按钮禁用；1002 → 红框+行内红字（非 Message 弹出）；提交 loading 防重复；成功跳 /departments 且 token 落 localStorage；已登录访问 /login 被重定向 |
| M6 大屏 | 七卡齐全（KPI×4/折线/时段/柱状/条形/占比/动态）；折线含数据点与面积填充；每卡 loading/空态；侧边栏「统计大屏」选中态；数值口径"不含已取消" |

---

## M5/M6 契约待对齐记录（V1.2 新增，站会销项）

> M5/M6 为 2026-09-09 迭代新增，**接口契约尚未落入 db/dto-contract.md / 拆解文档第 4 章**；落地前前端仅可按本设计稿 mock，禁止自行定稿字段。按 AGENTS.md 铁律二流程提请对齐。

| # | 待对齐项 | 影响接口 | 初拟方案 | 建议负责人 |
| --- | --- | --- | --- | --- |
| 12 | 管理端登录鉴权口径（patient_user 无 role 字段） | POST /auth/login 或新增 /admin/auth/login | 方案 A：复用 /auth/login + 种子预置管理员账号（如 admin），后端不加角色字段；方案 B：patient_user 加 role 列 + /admin 前缀拦截器校验角色 | 后端 A + 前端 C |
| 13 | 统计接口组（大屏七卡数据源） | GET /admin/stats/summary、/admin/stats/trend?days=7、/admin/stats/dept-rank、/admin/stats/doctor-top?limit=5、/admin/stats/title-ratio、/admin/stats/latest?limit=4 | 一个聚合接口（/admin/stats/dashboard 一次返回全部）vs 按卡拆分；响应字段沿用本设计稿 M6 表格示例数值结构（KPI/trend/deptRank/doctorTop/titleRatio/latest） | 后端 B + 前端 C |
| 14 | 图表库选型 | M6 全页 | ECharts 5 + vue-echarts（生态成熟、演示效果好）vs 纯 CSS/SVG 手写（零依赖、零基础可控）；选 ECharts 须组长评审新增依赖（1.4 基线规则） | 前端 C + 组长 |
| 15 | 统计数据时间窗口口径 | #13 接口组 | 趋势/排行固定"近 7 天（含当天）"；KPI 环比固定"较昨日/较上周"；是否做成可选参数本期不放开 | 后端 B |
