# 东软云医院挂号预约系统 · 任务分工说明书

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.0 |
| 编写日期 | 2026-09-08 |
| 上游文档 | 《项目拆解设计说明书》V1.2（第 4 章接口契约、第 9 章概要矩阵）、《需求分析说明书》V1.1 |
| 本文职责 | 将 S1–S12 与十天计划落实到**每个人、每一天**：职责卡、按天任务明细（含验收标准）、任务依赖、交付物与答辩分工 |
| 使用方式 | 每人只看自己的章节即可开工；逐天细化工作计划见根目录 `member-a/b/c/d/` 子目录（每人 10 份日计划 + AGENTS.md 对齐规则）；第 7 章签字确认后由组长归档 |

---

## 1. 分工总览

### 1.1 角色分工卡（姓名组内确认后回填）

| 成员 | 角色 | 一句话职责 | 负责故事 | 负责模块/工程 | 核心交付物 | 答辩讲解 |
| --- | --- | --- | --- | --- | --- | --- |
| A（组长） | 后端 · 架构/挂号域/中间件 | 搭骨架、管架构，攻坚挂号下单与高并发 | S8 S9 S10 | his-common、his-registration、Redis 三件套 | 工程骨架、统一响应体/异常、挂号域、缓存/锁/限流、压测（可选） | 架构演进、分布式锁与限流 |
| B | 后端 · 用户域/医院域/数据库 | 六表与全部查询/管理接口，接口文档唯一维护人 | S1–S7 S11 S12 | his-user、his-hospital | init.sql+种子数据、12 个接口、接口文档、数据库设计文档 | 数据库设计、接口规范 |
| C | 前端 · Vue3 管理端 | 管理端两大模块 + 联调期支援 App 端 | S11 S12（界面） | vue-admin 工程 | 管理端源码、运行说明、截图素材 | 管理端演示 |
| D | 前端 · 鸿蒙患者端 | 患者端全部页面与登录态体系 | S1–S10（界面） | harmony App 工程 | 鸿蒙源码、演示脚本、录屏素材 | App 全流程演示 |

> 工作量说明：后端 B 接口数最多但均为单表 CRUD；D 页面数最多（约 10 页）；C 第 3 天主体完工后第 5–6 天**必须支援 D**（见 4/5 章任务）；A 任务横跨全程但第 3 天后进入独立攻坚，可控。

### 1.2 互备与结对（防单点）

- 后端 A ↔ B 结对：每日 15 分钟代码走读，挂号域 B 必须熟悉、用户/医院域 A 必须熟悉；
- 前端 C ↔ D 互备：C 第 5–6 天支援 D 的静态页面；D 缺席时 C 顶演示；
- 任何人请假：站会上报，其当日任务由结对对象认领并在日报记录。

### 1.3 公共任务（全员每日，不重复写入个人章节）

| 时点 | 事项 |
| --- | --- |
| 09:00 | 站会 10 分钟：昨日完成 / 今日计划 / 阻塞求助 |
| 全天 | Git 提交（`feat:` / `fix:` / `optimize:` 前缀，每日至少 1 次） |
| 21:00 前 | 小组日报（轮值 A→B→C→D 循环，提交至 doc/daily/） |
| 第 2 天 15:00 | **接口评审会（全员必到）**：销《项目拆解设计说明书》4.8 节 11 项待对齐 |
| 第 9–10 天 | 各自答辩材料与演示彩排 |

### 1.4 任务依赖图（谁等谁）

```
D1  A骨架(his-api/common/port) ──→ D2 B开发S1/S2 ──┐
D2  15:00 接口契约定稿(评审会) ──→ C/D 用 Apifox mock 并行开工（不等后端写完）
D3  A挂号接口(S8初版) + B全量CRUD ──→ D3起 C 切真实接口
D4  A Redis(锁/限流) + B 缓存     ──→ D5–D6 App 全流程联调（关键路径：A→D）
D6  全流程跑通(M4) ──→ D7–D8 扩展与完善 ──→ D9–D10 交付
```

**关键路径**：A 的挂号域（D3–D4）→ D 的全流程联调（D6）。A、D 任一卡住立即站会升级。

---

## 2. 成员 A（组长 · 后端 · 挂号域与中间件）

### 2.1 职责范围

| 项 | 内容 |
| --- | --- |
| 负责故事 | S8 确认挂号、S9 挂号记录、S10 取消挂号 |
| 负责代码 | his-common（Result/异常/枚举/port 四接口）、JWT 与登录拦截器、his-registration 模块、Redis 三个实现类、（扩展）RocketMQ |
| 全局职责 | 仓库与分支策略、接口评审会主持、多模块拆分主导、架构决策与依赖版本把关、答辩彩排组织 |

### 2.2 按时间任务明细

**第 1 天（骨架期）**
- 主持启动会：过本分工表、分支模型（master 受保护 / dev / feature-*）、Apifox 工作区邀请全员；✅ 验收：全员入仓入工作区
- 建仓并配置权限；✅ 验收：每位成员可 pull/push dev
- 搭建单模块 `his-api` 骨架：pom 严格按拆解文档 1.4 基线锁版本、启动类、`application.yml`（8080 + context-path `/api`）；
- 编写 his-common 基础件：`Result<T>`、`PageDTO<T>`、`ErrorCode`（16 个码）、`BizException`、`GlobalExceptionHandler`；✅ 验收：`GET /hello` 返回 `{"code":200,...}`，`/doc.html` 可访问
- 定义 port 四接口（Cache / DistributedLock / RateLimit / MessageSender）+ Noop/Logging 默认实现；✅ 验收：可注入启动、切 yml 开关不报错

**第 2 天（拆分与认证底座）**
- `JwtUtil`（java-jwt 4.4.0，HS256，claims：userId/username/role，24h）；
- `AuthInterceptor` + WebMvc 配置：放行清单与拆解文档 4.1 一致（/auth/**、GET /departments、/doctors、/schedules），`/admin/**` 预留 ADMIN 校验位；✅ 验收：无 Token 访问 /members 返回 401
- 协同 B 联调 S1/S2 的 Token 全链路；
- 15:00 主持接口评审会：逐条销 4.8 节 11 项待对齐；✅ 验收：每项都有结论并更新文档 + Apifox
- 17:00–18:00 主导多模块拆分（按拆解文档 2.3 五步清单）；✅ 验收：五模块结构成型，拆分前全部 Apifox 用例回归通过

**第 3 天（挂号域初版）**
- S8 初版（不含锁）：`RegistrationCreateReqDto/RespDto`、订单号工具（`yyyyMMddHHmmssSSS`+6 位随机）、编排链（校验就诊人归属 → 原子扣号 UpdateChain → 快照落库）；
- S9：`GET /registrations/my`（按创建时间倒序）；
- S10：`POST /registrations/{orderNo}/cancel`，`@Transactional` 内状态流转 + 号源 +1 回补；
- ✅ 验收：Apifox 用例含正常路径与 3001/3005/3006/3004 错误分支全通过

**第 4 天（Redis 接入）**
- 实现 `RedisCacheService` / `RedisSetnxLockService`（finally 释放）/ `RedisCounterRateLimitService`；yml `his.middleware.redis.enabled` 切换验证；
- 挂号编排插入"限流 → 锁 → 校验 → 扣号 → 落库 → MessageSender"；
- ✅ 验收（里程碑 M3）：并发 10 次同一"患者+医生+日期+时段"仅 1 单成功；60 秒内第 6 次请求返回 3003；与 B 联调科室缓存二查命中

**第 5 天（联调支撑）**
- 修复 App 联调反馈的挂号链路缺陷；
- 编写排班造数 SQL（每医生 7 天 × 上/下午）；提交至 db/ 目录；
- 15 分钟代码走读：带 B 过一遍挂号域全链路。

**第 6 天（全流程串联）**
- 事务复核：取消后 `left_quota+1` 且 `status='已取消'`（按订单内 schedule_id 直达回补，拆解文档 6.5-①）；构造扣号后异常验证回滚；
- 整理 Apifox 一键回归集合（19 个接口全量）；✅ 验收（M4 前置）：后端侧全绿

**第 7 天（扩展，可选）**
- Spring AI 导诊接口 + SSE 流式输出（视主线进度决定，主线优先）；

**第 8 天（MQ 扩展 + 兜底）**
- `RocketMQMessageSender`（topic：`his_registration_success`），未部署 MQ 时保留 Logging 实现可降级演示；
- 全局异常兜底复查（NPE、参数缺失、Redis 断连时的降级路径）。

**第 9 天（压测与答辩材料）**
- JMeter 压测挂号链路（并发 50 / 持续 1 分钟），记录 QPS、错误率，截图入 PPT（可选加分项）；
- PPT 撰写：架构演进三阶段图、缓存/锁/限流原理图；组织演示彩排第一轮。

**第 10 天（答辩）**
- 答辩讲解：架构演进、分布式锁与限流；准备模拟面试问题（缓存穿透/雪崩/击穿、锁误删、事务边界）。

### 2.3 依赖关系

- **我等**：B 的 S1/S2（Token 联调）、用户/医院域接口（挂号编排调用）、D 的联调缺陷反馈；
- **等我**：全组（D1 骨架、D2 评审会与拆分）、D（D3–D4 挂号接口与 Redis）、答辩架构章节。

### 2.4 交付物清单

Git 仓库与分支策略、his-common 全部基础件、his-registration 模块、Redis 三件套、排班造数 SQL、压测记录（可选）、PPT 架构/高并发章节。

---

## 3. 成员 B（后端 · 用户域/医院域/数据库）

### 3.1 职责范围

| 项 | 内容 |
| --- | --- |
| 负责故事 | S1 注册、S2 登录、S3 退出、S4 科室列表、S5 医生列表、S6 排班、S7 就诊人、S11 科室管理、S12 医生管理 |
| 负责代码 | his-user 模块（Auth/Member）、his-hospital 模块（Department/Doctor/Schedule 查询 + Admin CRUD） |
| 全局职责 | 六表 DDL 与种子数据唯一维护人、Knife4j/Apifox 接口文档维护、日报汇总归档 |

### 3.2 按时间任务明细

**第 1 天（数据库日）**
- 出《环境核对清单》并逐人签字：JDK17、Maven 阿里云镜像、MySQL8、Redis、Node18+、DevEco；✅ 验收：全员环境可跑
- 按拆解文档 6.3 编写 `db/init.sql`（六表 + 注释 + 索引）+ 种子数据（≥2 科室、每科室 2 医生、每医生 7 天排班、1 个测试账号）；✅ 验收：一键执行成功且能查到数据

**第 2 天（登录注册 + 首批查询）**
- S1 注册：用户名唯一校验（1001）、Hutool BCrypt 加密落库；
- S2 登录：校验（1002）、返回 `LoginRespDto`（绝不返回密码），Token 用 A 的 JwtUtil；
- S4 `GET /departments`、S5 `GET /doctors?deptId=`（按职称权重排序：主任→副主任→主治）；
- 参加接口评审会；配合 A 完成多模块拆分（迁移 user/hospital 两包）；✅ 验收：5 个接口 Apifox 通过，拆分后回归通过

**第 3 天（CRUD 高产日）**
- S6 `GET /schedules?doctorId=` 近 7 天（无排班返回空数组）；
- S7 `POST /members`（上限 3 人→2001、IdcardUtil 校验→2002）、`GET /members`；
- S3 `POST /auth/logout`；
- S11 `/admin/departments` 四接口（`paginate` → `PageDTO`）；
- S12 `/admin/doctors` 四接口（deptId 不存在→4001）；
- ✅ 验收：Apifox 全绿；对照拆解文档 4.3–4.6 抽查字段一致性

**第 4 天（科室缓存）**
- 科室查询接入 `CacheService`（key 按 7.1 节：`dept:list:{category}:{delmark}`，TTL 1800s+抖动；**只缓存 List 不缓存 Page**）；
- 管理端科室增/改/删后 `evictByPrefix`；✅ 验收（M3 协同）：二查命中不回源；后台改科室后前台立即可见

**第 5 天（文档与支撑）**
- Knife4j 注解补全（`@Tag`/`@Operation`/`@Schema`）；
- 修复联调缺陷；支援 A 完善造数数据。

**第 6 天（文档交付稿）**
- Apifox 导出接口文档（markdown）入库 `doc/`；
- 更新拆解文档 4.8 待对齐表状态为"已定"；✅ 验收：接口文档成为交付物清单第 3 项的一部分

**第 7 天（缓冲/扩展）**
- 协同 A 准备 AI 导诊测试数据，或提前做第 8 天边界复查（二选一，主线优先）。

**第 8 天（边界完善）**
- 全部 ReqDto 参数校验注解复查（@NotBlank/@Pattern）；
- 逻辑删除专项验证：删除科室/医生后患者端不可见且缓存已清。

**第 9 天（数据库设计文档）**
- 编写《数据库设计文档》（ER 图、字段字典、三范式与快照冗余说明）——交付物；
- PPT 素材：数据库设计与接口规范截图。

**第 10 天（答辩）**
- 答辩讲解：数据库设计思路、统一响应体/异常/DTO 规范。

### 3.3 依赖关系

- **我等**：A 的骨架与 JwtUtil（D1–D2）、接口评审会字段定稿（D2）；
- **等我**：A 的挂号编排（member/schedule 校验）、C 的管理端真实联调（D3）、D 的患者端联调（D5）。

### 3.4 交付物清单

`db/init.sql` + 种子数据、his-user 模块、his-hospital 模块、接口文档（Apifox 导出）、《数据库设计文档》、日报归档。

---

## 4. 成员 C（前端 · Vue3 管理端）

### 4.1 职责范围

| 项 | 内容 |
| --- | --- |
| 负责故事 | S11 科室管理、S12 医生管理（Web 管理端界面） |
| 负责工程 | vue-admin（Vue3 + Element Plus + Vite + Vue Router + Pinia） |
| 全局职责 | 管理端运行说明文档、答辩截图素材库（第 5 天起兼） |

### 4.2 按时间任务明细

**第 1 天（脚手架）**
- Node18+ 环境，Vite 创建工程，集成 Element Plus / Router / Pinia；
- 布局框架：侧边菜单（科室管理/医生管理）+ 顶栏 + 内容区；
- axios 封装：baseURL `http://localhost:8080/api`、`code!==200` 统一 `ElMessage(message)`、401 跳登录占位页、全局 loading；✅ 验收：页面框架可跑

**第 2 天（mock 先行）**
- Apifox 导入评审会接口契约，开启本地 mock；
- 科室管理页：分页表格 + 新增/编辑 Dialog（名称必填校验）+ 删除二次确认；✅ 验收：**不依赖后端**用 mock 完整演示增删改查

**第 3 天（切真实接口）**
- 对接 B 的 S11 真实接口：分页、增删改、业务错误提示；
- 医生管理页：表格（含科室名称列）+ 表单（科室下拉必选、职称下拉：主任医师/副主任医师/主治医师）+ 删除确认；
- ✅ 验收：管理端对真实数据库增删改查全通

**第 4 天（回归与 UX）**
- 管理端回归：配合 B 验证"改科室 → 前台立即可见"；
- UX 完善：loading、空态、表单重置、分页参数保持。

**第 5–6 天（支援 App 端 + 素材）** ⭐ 硬性安排
- **支援 D**：挂号成功页、挂号记录页等静态页面搭建（数据绑定仍由 D 完成）；
- 管理端缺陷修复；采集答辩截图素材（入库 doc/screenshots/）。

**第 7 天（扩展，可选）**
- AI 导诊对话页接入（若 A 完成 SSE 接口），或继续支援 D。

**第 8 天（健壮性）**
- 回归：后端停服/超时时的 UI 兜底提示；401 统一跳转验证。

**第 9 天（文档与 PPT）**
- 管理端运行说明（README：`npm install` / `npm run dev`、Node 版本要求）；
- PPT 素材：管理端功能截图与亮点整理。

**第 10 天（答辩）**
- 现场演示：科室/医生管理全流程操作。

### 4.3 依赖关系

- **我等**：接口契约（D2 15:00 评审会后 mock 即可开工，不等后端实现）、B 的真实接口（D3）；
- **等我**：D（第 5–6 天静态页面支援）、答辩管理端演示。

### 4.4 交付物清单

vue-admin 源码（不含 node_modules）、管理端运行说明、答辩截图素材库。

---

## 5. 成员 D（前端 · 鸿蒙患者端）

### 5.1 职责范围

| 项 | 内容 |
| --- | --- |
| 负责故事 | S1–S10 患者端全部页面（含 S3 退出与 withLogin 守卫） |
| 负责工程 | HarmonyOS App（ArkTS + ArkUI，API 13，@ohos/axios 2.2.10） |
| 全局职责 | 演示脚本与真机准备、全流程演示主操 |

### 5.2 按时间任务明细

**第 1 天（环境）**
- DevEco Studio 6.1 + 模拟器（API 13）跑通模板工程；
- 申请 `ohos.permission.INTERNET`；引入 @ohos/axios；✅ 验收：模拟器页面能请求到本机后端 `/hello`（真机需同网段，备用模拟器）

**第 2 天（网络层与登录注册页）**
- axios 封装：Token 存 AppStorage、请求拦截器注入 `Authorization` 头、`code!==200` 统一 Toast、网络异常统一提示"网络连接异常，请检查网络"；
- 注册页（确认密码一致校验、1001 用户名重复提示）、登录页（成功存 Token 跳首页）；✅ 验收：对 mock/真实接口均可注册登录

**第 3 天（浏览主线页面）**
- 首页科室列表（点击进入医生列表）；
- 医生列表页（姓名/职称/擅长，按后端排序展示）；
- 排班页：近 7 天列表、剩余号源数、已满显示"已满"置灰不可选、无排班显示"暂无排班"。

**第 4 天（就诊人与确认挂号）**
- 就诊人页：列表 + 添加表单（姓名/性别/身份证/手机号/出生日期，身份证前端格式校验）；
- 确认挂号页：就诊人/科室/医生/日期/时段/挂号费完整核对 + 提交（3001/3002/3003 错误 Toast）。

**第 5 天（登录态与联调）**
- `withLogin` 守卫封装（无 Token 跳登录页），需登录页面全部接入；
- 与后端联调 S1–S7，缺陷清单当日反馈 A/B；✅ 验收：主流程（浏览→就诊人）真接口跑通

**第 6 天（全流程收口）** ⭐ 关键路径
- 挂号成功页（订单号展示/复制）；我的挂号记录页（倒序、状态标签：待就诊/已取消）；
- 取消挂号：二次确认弹窗 → 成功刷新列表；
- S3 退出登录：清 Token 回登录页，返回键验证守卫生效；
- ✅ 验收（M4）：模拟器/真机完整走通 S4→S5→S6→S7→S8→S9→S10

**第 7 天（扩展，可选）**
- AI 管家"小医"对话页（若后端 SSE 就绪）。

**第 8 天（回归与素材）**
- 全页面回归、异常 Toast 统一；采集截图与录屏素材（答辩与运行说明用）。

**第 9 天（演示准备）**
- 编写演示脚本（页面点击路径 + 讲解词，对应验收演示要求：选科室→选医生→选排班→确认挂号→查看记录→取消）；
- 真机准备 + 备用模拟器双保险；PPT 患者端章节素材。

**第 10 天（答辩）**
- App 全流程现场演示（主操），协助回答前端问题。

### 5.3 依赖关系

- **我等**：接口契约（D2 评审会）、A 的挂号接口与 Redis（D3–D4）、B 的域接口（D2–D3）、C 的静态页面支援（D5–D6）；
- **等我**：全组（D6 M4 全流程真联通是答辩演示前提）、答辩演示主操。

### 5.4 交付物清单

鸿蒙工程源码（不含 oh_modules/build）、演示脚本、截图与录屏素材、App 端运行说明。

---

## 6. 里程碑责任人对照

| 里程碑 | 时点 | 验收项 | 第一责任人 | 协同 |
| --- | --- | --- | --- | --- |
| M1 | 第 1 天末 | 骨架可启动、/doc.html、建表成功 | A | B |
| M2 | 第 3 天末 | 多模块回归通过、S4–S12 接口全通 | B | A |
| M3 | 第 4 天末 | 缓存命中、并发拦截、限流生效 | A | B |
| M4 | 第 6 天末 | App 全流程真联通 | D | A/B/C |
| M5 | 第 8 天末 | 取消事务、清缓存验证、（扩展）MQ | A | B/D |
| M6 | 第 10 天 | 交付物齐全、答辩通过 | 全员 | A 归档 |

## 7. 个人任务认领确认表

| 成员 | 角色 | 已阅读并认领本人任务（签字） | 日期 | 备注 |
| --- | --- | --- | --- | --- |
|  | A（组长/后端） |  |  |  |
|  | B（后端） |  |  |  |
|  | C（前端 Vue） |  |  |  |
|  | D（前端鸿蒙） |  |  |  |

> 认领规则：签字即承诺按验收标准交付；任务变更须经组长在站会确认并同步更新本文件（提交 commit：`optimize: 任务分工调整`）。
