# 东软云医院挂号预约系统 · 需求分析说明书

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.1 |
| 编写日期 | 2026-09-08 |
| 适用对象 | 产品 / 设计 / 开发 / 测试 / 运维 |
| 文档密级 | 项目内部 |
| 维护人 | 第14组（张家俊组） |
| 需求来源 | 《东软云医院挂号预约系统》用户故事（S1–S12） |
| 参考依据 | 《东华理工大学202609实训 SpringBoot+鸿蒙项目过程文档》 |

---

## 1. 项目背景

### 1.1 实训背景

本项目为**东华理工大学 202609 项目实训**课题（实训地点：东华理工大学广兰校区软件楼 506，实训周期 2026.09.07–2026.09.18，共 10 天、80 学时）。实训以东软（Neusoft）真实企业级项目为蓝本，由项目经理及开发工程师带队，学生按 4–6 人一组、明确角色分工，模拟"需求分析 → 数据库设计 → 开发 → 联调 → 测试 → 答辩"的完整软件工程流程，并以阿里云效 Codeup（或 Gitee）上的 Git 仓库提交记录作为过程性考核依据。

### 1.2 业务背景

传统医院现场挂号存在排队时间长、号源信息不透明、家属代挂号不便等问题。本项目以"东软云医院挂号预约系统"为切入点，为患者提供移动端（鸿蒙 App）在线挂号预约服务，同时为医院后台管理员提供基于 Web 管理端的科室与医生信息维护能力，实现挂号流程的线上化、自助化。

### 1.3 业务方向

医疗（互联网医院 / 挂号预约）

### 1.4 核心价值

1. **挂号线上化**：患者无需到院排队，在 App 上即可完成"选科室 → 选医生 → 选排班 → 确认挂号"的全流程。
2. **家庭代挂号**：支持患者为家人（最多 3 名就诊人）进行挂号预约，覆盖老人、儿童等不便操作手机的群体。
3. **号源透明化**：排班与剩余号源实时可见，已约满时段明确标注，减少无效等待。
4. **后台可维护**：管理员可随时维护科室与医生数据，变化后自动清除缓存，保证前台数据一致。
5. **工程化练兵**：作为企业级模拟项目，覆盖 Redis 缓存、分布式锁、接口限流等高并发治理手段，以及 Git 团队协作全流程。

### 1.5 技术背景与技术选型

依据过程文档的统一环境要求，本项目采用"一个后端 + 两套前端"的架构：

| 端 | 技术栈 | 版本要求 |
| --- | --- | --- |
| 后端 API（his-api） | SpringBoot + MyBatis-Flex + MySQL + JWT + Knife4j | SpringBoot 3.1.5、JDK 17、Maven 3.9.11、MyBatis-Flex 1.8.8、MySQL 8.0、java-jwt 4.4.0、Knife4j 4.4.0 |
| 缓存与高并发 | Redis（Cache-Aside 缓存、SETNX 分布式锁、计数器限流） | Redis 7.2.x |
| 患者端（App） | HarmonyOS ArkTS + ArkUI，DevEco Studio 开发，@ohos/axios 网络请求 | HarmonyOS 5.0.1（API 13）、DevEco Studio 6.1.0、@ohos/axios 2.2.10 |
| 管理端（Web） | Vue3 + Element Plus + Vite + Vue Router + Pinia | Node.js 18+ |
| 接口联调 | Apifox / Postman（token 存环境变量）、Knife4j 在线文档（/doc.html） | — |

部署形态：后端服务默认端口 **8080**，上下文路径 **/api**，接口文档地址 `http://localhost:8080/doc.html`。

### 1.6 建设周期

按实训教学日历，10 天开发节奏为：第 1 天项目启动与环境搭建 → 第 2 天数据库设计与登录注册接口 → 第 3 天核心业务开发与 Vue 管理端 → 第 4 天 Redis 与高并发优化 → 第 5–6 天鸿蒙端开发与接口联调 → 第 7–8 天扩展专题与项目完善 → 第 9–10 天技术分享、模拟面试与项目答辩。详见第 6 章"实施计划与交付"。

---

## 2. 用户角色

### 2.1 角色定义

| 角色 | 数量级 | 使用终端 | 主要诉求 |
| --- | --- | --- | --- |
| 患者（PATIENT） | 数千 | 鸿蒙 App（患者端） | 注册登录、浏览科室与医生、查看排班、为本人或家人挂号、查看/取消挂号记录 |
| 医院后台管理员（ADMIN） | 少量 | Vue3 Web 管理端 | 维护科室信息、维护医生信息 |

### 2.2 权限矩阵

| 功能 | 患者（App） | 管理员（Web 管理端） |
| --- | --- | --- |
| 注册 / 登录 / 退出 | ✅ | — |
| 查看科室列表 | ✅ | — |
| 按科室查看医生 | ✅ | — |
| 查看医生排班 | ✅ | — |
| 添加/管理就诊人 | ✅ | — |
| 确认挂号 | ✅ | — |
| 查看/取消挂号记录 | ✅ | — |
| 科室管理（增删改查） | ❌ | ✅ |
| 医生管理（增删改查） | ❌ | ✅ |

> 说明：退出登录后患者无法访问任何需登录才能使用的功能（依据 S3）。

---

## 3. 功能模块（按用户故事）

> 功能点全部来源于用户故事 S1–S12；各故事附"实现要点"系依据过程文档的统一技术方案，仅作实现约束，不新增功能。

### 3.1 模块一：用户与认证（F-USER）

#### 3.1.1 用户注册【S1】（优先级：高）

- **用户故事**：作为一名患者，我想在 App 上注册一个账号，以便使用挂号预约功能。
- **验收标准**：
  - 输入用户名、密码、确认密码即可完成注册
  - 注册成功后自动跳转到登录页面
  - 用户名不可重复，重复时给出明确提示
- **实现要点**：注册/登录接口由后端统一提供（JWT 方案）；用户名唯一性由后端校验并返回明确错误信息；密码不得明文回传前端。

#### 3.1.2 用户登录【S2】（优先级：高）

- **用户故事**：作为已注册的患者，我想输入用户名和密码登录系统，以便访问个人中心和挂号功能。
- **验收标准**：
  - 用户名和密码正确时登录成功，返回 JWT Token
  - Token 有效期为 24 小时，过期后需重新登录
  - 用户名或密码错误时给出明确提示
- **实现要点**：JWT 签发基于 java-jwt 4.4.0；鸿蒙端登录成功后将 Token 存入 AppStorage，axios 请求拦截器统一在请求头 `Authorization` 中携带；登录响应中的密码字段不得返回前端。

#### 3.1.3 退出登录【S3】（优先级：高）

- **用户故事**：作为已登录的患者，我想安全退出当前账号，以便切换账号或保护个人隐私。
- **验收标准**：
  - 点击退出后清除本地存储的 Token
  - 退出后跳转到登录页面
  - 退出后无法访问需要登录才能使用的功能
- **实现要点**：鸿蒙端封装 `withLogin` 守卫——访问需登录功能时检查 AppStorage 中的 Token，缺失则路由跳转登录页。

### 3.2 模块二：挂号核心流程（F-APPOINTMENT）

#### 3.2.1 查看科室列表【S4】（优先级：高）

- **用户故事**：作为一名患者，我想在首页查看所有科室分类，以便找到我要挂号的科室。
- **验收标准**：
  - 以列表形式展示所有科室，包含科室名称和分类
  - 点击某个科室跳转到该科室的医生列表页面
  - 科室数据首次加载后缓存 30 分钟，提升访问速度
- **实现要点**：采用 Redis Cache-Aside（旁路缓存）模式，缓存 key 形如 `dept:list:{科室分类}:{删除标记}`，过期时间 1800 秒；缓存命中直接返回、未命中查 MySQL 后回填；只缓存 List 不缓存 Page 对象。

#### 3.2.2 按科室查看医生【S5】（优先级：高）

- **用户故事**：作为一名患者，我想选择一个科室后查看该科室的所有医生，以便选择我信任的医生进行挂号。
- **验收标准**：
  - 展示医生姓名、职称（主任医师 / 副主任医师 / 主治医师）
  - 展示医生的擅长领域简介
  - 医生列表按职称高低排序
- **实现要点**：医生数据归属于科室（医生表含所属科室外键），支持按职称级别排序输出。

#### 3.2.3 查看医生排班【S6】（优先级：高）

- **用户故事**：作为一名患者，我想查看某位医生未来 7 天的排班情况，以便选择合适的日期和时段去就诊。
- **验收标准**：
  - 以列表或日历形式展示近 7 天的排班
  - 每个排班显示日期、时段（上午 / 下午）、剩余号源数
  - 已约满的时段显示为"已满"且不可选择
  - 未排班的日期显示为"暂无排班"
- **实现要点**：排班为独立数据实体（医生 × 日期 × 时段 × 号源），号源余量随挂号/取消动态增减；未排班日期返回空集由前端渲染为"暂无排班"。

#### 3.2.4 添加就诊人【S7】（优先级：中）

- **用户故事**：作为已登录的患者，我想添加家人作为就诊人，以便为家人进行挂号预约。
- **验收标准**：
  - 填写就诊人姓名、性别、身份证号、手机号、出生日期
  - 身份证号格式进行校验，格式错误时给出提示
  - 每位患者最多添加 3 个就诊人
  - 添加成功后可在就诊人列表中看到新记录
- **实现要点**：就诊人（患者档案）为独立实体，通过患者 ID 关联归属；身份证号格式校验在前后端双侧执行。

#### 3.2.5 确认挂号【S8】（优先级：高）

- **用户故事**：作为已完成科室、医生、排班、就诊人选择的患者，我想确认挂号信息并提交，以便完成预约挂号。
- **验收标准**：
  - 确认页面展示完整信息：就诊人姓名、科室、医生、就诊日期、时段、挂号费
  - 点击确认后生成唯一订单号
  - 同一患者 + 同一医生 + 同一日期 + 同一时段只能挂号一次，重复时给出提示
  - 每位患者每分钟最多发起 5 次挂号请求，超出时提示"操作频繁，请稍后再试"
  - 挂号成功后跳转到"挂号成功"页面并显示订单号
- **实现要点**（依据过程文档统一方案）：
  - **防重复挂号**：Redis 分布式锁（SETNX / setIfAbsent），锁 key 为 `reg:lock:{患者ID}:{医生ID}:{日期}:{时段}`，兜底过期 30 秒，抢锁失败即拒绝；锁必须在 `finally` 中释放。
  - **限流**：Redis 计数器自增，key 精确到"患者 + 分钟"（`reg:limit:{患者ID}:{yyyyMMddHH}`），60 秒自动过期，超过 5 次拒绝。
  - **订单号生成**：`yyyyMMddHHmmssSSS` 时间戳 + 6 位随机数字，保证唯一。

#### 3.2.6 查看挂号记录【S9】（优先级：高）

- **用户故事**：作为已登录的患者，我想查看我的所有挂号记录，以便了解历史就诊情况和进行后续操作。
- **验收标准**：
  - 按创建时间倒序排列，最新的在前
  - 每条记录显示：订单号、科室名称、医生姓名、就诊日期、就诊时段、状态
  - 状态包括：待就诊、已取消
- **实现要点**：挂号记录表冗余存储下单时刻的科室名称、医生姓名等快照字段，避免关联查询受后台数据变更影响。

### 3.3 模块三：取消与后台管理（F-CANCEL / F-ADMIN）

#### 3.3.1 取消挂号【S10】（优先级：中）

- **用户故事**：作为有挂号记录的患者，我想取消尚未就诊的挂号记录，以便释放号源给其他有需要的患者。
- **验收标准**：
  - 只有状态为"待就诊"的记录可以取消
  - 点击取消后弹出二次确认提示
  - 取消成功后状态更新为"已取消"
  - 取消后对应的号源数量加 1 恢复
- **实现要点**：状态流转（待就诊 → 已取消）与号源回补应在同一数据库事务中完成，保证号源数据准确。

#### 3.3.2 后台管理 — 科室管理【S11】（优先级：中）

- **用户故事**：作为医院后台管理员，我想管理科室信息，以便维护最新的科室数据。
- **验收标准**：
  - 以表格形式展示所有科室列表
  - 支持新增科室（填写名称、分类、简介）
  - 支持编辑科室信息
  - 支持删除科室
  - 科室数据发生变化后自动清除缓存，确保前台看到最新数据
- **实现要点**：管理端基于 Vue3 + Element Plus 表格组件实现增删改查；后端在科室新增/修改/删除时执行"写时删缓存"（删除 `dept:list:*` 相关 key），下次查询自动回源数据库。

#### 3.3.3 后台管理 — 医生管理【S12】（优先级：中）

- **用户故事**：作为医院后台管理员，我想管理医生信息，以便维护最新的医生数据。
- **验收标准**：
  - 以表格形式展示所有医生列表，包含所属科室
  - 支持新增医生（填写姓名、所属科室、职称、擅长领域）
  - 支持编辑医生信息
  - 支持删除医生
  - 医生必须归属于某个科室，不可独立存在
- **实现要点**：医生实体的"所属科室"为必填外键，后端校验医生必须挂靠在已有科室下；删除采用逻辑删除标记（delmark）机制。

### 3.4 功能优先级汇总

| 编号 | 用户故事 | 所属模块 | 优先级 |
| --- | --- | --- | --- |
| S1 | 用户注册 | 用户与认证 | 高 |
| S2 | 用户登录 | 用户与认证 | 高 |
| S3 | 退出登录 | 用户与认证 | 高 |
| S4 | 查看科室列表 | 挂号核心流程 | 高 |
| S5 | 按科室查看医生 | 挂号核心流程 | 高 |
| S6 | 查看医生排班 | 挂号核心流程 | 高 |
| S7 | 添加就诊人 | 挂号核心流程 | 中 |
| S8 | 确认挂号 | 挂号核心流程 | 高 |
| S9 | 查看挂号记录 | 挂号核心流程 | 高 |
| S10 | 取消挂号 | 取消与后台管理 | 中 |
| S11 | 科室管理 | 取消与后台管理 | 中 |
| S12 | 医生管理 | 取消与后台管理 | 中 |

### 3.5 核心数据实体

依据过程文档的数据库建模要求，系统在 MySQL 8 中建立六张核心业务表，所有表须有明确的表注释和字段注释：

| 数据实体 | 支撑的用户故事 | 关键字段（示例） |
| --- | --- | --- |
| 用户（患者账号） | S1、S2、S3 | 用户名（唯一）、密码、注册时间 |
| 科室 | S4、S11 | 科室名称、科室分类、简介、逻辑删除标记 |
| 医生 | S5、S12 | 姓名、所属科室（外键，必填）、职称、擅长领域 |
| 排班 | S6 | 医生、日期、时段、号源总数/剩余数 |
| 就诊人 | S7 | 姓名、性别、身份证号、手机号、出生日期、归属患者 |
| 挂号记录 | S8、S9、S10 | 订单号（唯一）、就诊人快照、科室、医生、就诊日期、时段、挂号费、状态 |

---

## 4. 业务流程

### 4.1 注册登录流程（S1 → S2）

```
[患者] → 打开鸿蒙 App → 注册（用户名/密码/确认密码）
       → 后端校验用户名唯一性（重复则返回明确提示）
       → 注册成功 → 自动跳转登录页
       → 输入用户名 + 密码 → 后端校验通过 → 签发 JWT Token（有效期 24h）
       → Token 存入 AppStorage → 后续请求经 axios 拦截器统一携带 Authorization 头
       → 进入首页，可访问个人中心和挂号功能
```

### 4.2 核心挂号流程（S4 → S8）

```
[患者] → 首页查看科室列表（Redis 缓存 30 分钟，未命中回源 MySQL）
       → 点击科室 → 查看该科室医生列表（按职称排序）
       → 选择医生 → 查看近 7 天排班（日期/时段/剩余号源）
       → 选择有剩余号源的时段（"已满"不可选，未排班显示"暂无排班"）
       → 选择就诊人（本人或已添加的家人，最多 3 人）
       → 确认页核对：就诊人、科室、医生、日期、时段、挂号费
       → 提交，后端依次执行：
           ├─ 限流校验：Redis 计数器，每患者每分钟 ≤ 5 次，超出拒绝
           ├─ 分布式锁：reg:lock:{患者}:{医生}:{日期}:{时段}，抢锁失败拒绝（防重复挂号）
           ├─ 号源校验与扣减
           └─ 生成唯一订单号（时间戳 + 随机数）→ 落库 → 跳转"挂号成功"页面
```

### 4.3 取消挂号流程（S9 → S10）

```
[患者] → 查看挂号记录（按创建时间倒序）
       → 选择状态为"待就诊"的记录 → 点击取消
       → 弹出二次确认提示 → 确认取消
       → 同一事务内：记录状态更新为"已取消" + 对应号源数量 +1 恢复
```

### 4.4 后台数据维护流程（S11 / S12）

```
[管理员] → Vue3 管理端以表格查看科室/医生列表
        → 新增/编辑/删除科室（名称、分类、简介）
        → 新增/编辑/删除医生（姓名、所属科室、职称、擅长领域；科室必填）
        → 数据变更 → 同步清除 Redis 科室缓存 → 前台下次查询回源，展示最新数据
```

### 4.5 退出登录流程（S3）

```
[患者] → 点击退出 → 清除 AppStorage 中的 Token
       → 跳转登录页 → 需登录的功能经 withLogin 守卫拦截，不可访问
```

---

## 5. 非功能性需求

### 5.1 性能需求

| 指标 | 目标 | 实现手段 |
| --- | --- | --- |
| 科室数据访问 | 首次加载后缓存 30 分钟 | Redis 7.2 Cache-Aside，key 过期 1800s，命中免查库 |
| 挂号接口限流 | 每位患者每分钟最多 5 次 | Redis 计数器自增 + 60s 过期，超出提示"操作频繁，请稍后再试" |
| 重复请求防护 | 同患者+同医生+同日期+同时段仅一单 | Redis SETNX 分布式锁，30s 兜底过期，finally 释放 |
| 高并发验证 | 关键接口可承受压测 | 实训期间以 JMeter 对挂号链路进行压测验证 |

设计上需关注缓存穿透、缓存雪崩、缓存击穿三类典型问题的防护（空值缓存/过期时间打散等），此为实训明确要求的工程素养点。

### 5.2 安全需求

- 鉴权：JWT Token（java-jwt 4.4.0），有效期 24 小时，过期后需重新登录。
- 传输携带：鸿蒙端 Token 存于 AppStorage，axios 请求拦截器统一注入 `Authorization` 头。
- 退出安全：退出登录后清除本地 Token，需登录功能不可访问。
- 数据脱敏：登录等接口的响应不得返回密码字段。
- 数据校验：身份证号格式校验（S7）、用户名唯一性校验（S1）。
- 业务防护：同一患者 + 同一医生 + 同一日期 + 同一时段重复挂号拦截（S8）。

### 5.3 数据一致性需求

- 取消挂号后号源数量加 1 恢复，状态流转与号源回补在同一事务内完成（S10）。
- 后台科室/医生数据变更后自动清除缓存，前台立即可见最新数据（S11、S12）。
- 分布式锁必须通过 `try-finally` 保证释放，防止死锁。

### 5.4 易用性需求

- 已约满时段显示"已满"且不可选择；未排班日期显示"暂无排班"（S6）。
- 关键操作（取消挂号）提供二次确认提示（S10）。
- 挂号记录按创建时间倒序排列，最新在前（S9）。
- 鸿蒙端统一异常提示：网络断开/超时提示"网络连接异常，请检查网络"；业务消息统一以 Toast 弹出。

### 5.5 环境与技术约束

| 类别 | 约束 |
| --- | --- |
| 后端 | JDK 17、Maven 3.9.11（阿里云镜像）、SpringBoot 3.1.5、MyBatis-Flex 1.8.8、MySQL 8.0、Redis 7.2.x、Knife4j 4.4.0 |
| 服务配置 | 端口 8080，上下文路径 /api，接口文档 /doc.html |
| 患者端 | DevEco Studio 6.1.0+、HarmonyOS 5.0.1（API 13）、ArkTS/ArkUI、@ohos/axios 2.2.10，需申请网络权限 `ohos.permission.INTERNET` |
| 管理端 | Vue3 + Element Plus + Vite + Vue Router + Pinia，Node.js 18+ |
| 数据库 | 所有表必须有明确的表注释和字段注释；交付 MySQL 8 建表脚本（.sql） |
| 安装路径 | 开发工具与 JDK 安装路径不得包含中文、空格、特殊字符 |

### 5.6 兼容性需求

- 患者端：HarmonyOS 5.0.1（API 13）模拟器及真机。
- 管理端：主流现代浏览器（Chrome / Edge）。
- 后端：Windows 本地部署运行，通过启动脚本一键启动。

### 5.7 协作与过程约束

- 版本管理：代码托管于阿里云效 Codeup（或 Gitee），组长将组员添加为"开发者"角色。
- 提交规范：每日提交，commit message 使用 `feat: xxx` / `fix: xxx` / `optimize: xxx` 前缀；组内需解决合并冲突。
- 联调规范：接口先行，使用 Apifox/Postman 联调，登录 Token 存入环境变量统一携带。
- 过程文档：每日提交小组日报；需求分析、数据库设计、接口文档等过程文档随项目交付。

---

## 6. 实施计划与交付

### 6.1 开发计划（依据实训教学日历，共 10 天 / 80 学时）

| 天数 | 主题 | 与本需求的对应关系 |
| --- | --- | --- |
| 第 1 天 | 项目启动、分组、环境搭建、云效 Codeup 实践 | 全局环境约束（5.5、5.7）落地 |
| 第 2 天 | 数据库设计、后端基础、登录注册接口（JWT） | 数据实体（3.5）、S1/S2/S3 |
| 第 3 天 | 挂号流程拆解、RESTful 接口设计、科室查询与排班、挂号接口、Vue 管理端开发 | S4–S9、S11/S12（管理端） |
| 第 4 天 | Redis 缓存、防重复挂号、限流优化 | S4（缓存）、S8（锁+限流） |
| 第 5 天 | 鸿蒙端基础：首页布局、科室选择页、登录页、联调 | S1/S2/S4 患者端页面 |
| 第 6 天 | 鸿蒙端核心开发与接口联调，挂号全流程跑通 | S5–S9 患者端全流程 |
| 第 7 天 | AI 导诊扩展专题（见第 7 章） | 范围外扩展 |
| 第 8 天 | 项目完善、Bug 修复、消息队列扩展专题 | S10 及收尾；范围外扩展 |
| 第 9 天 | 项目整合、答辩准备（PPT、分工表） | 交付物准备 |
| 第 10 天 | 模拟面试、项目答辩与总结 | 验收 |

### 6.2 交付物清单

1. **答辩 PPT**：含组长/组员姓名与分工、项目背景/功能/架构介绍、核心功能运行截图、项目总结与亮点。
2. **答辩项目压缩包**：后端源码（不含 target 目录）、Vue 管理端源码（不含 node_modules）、鸿蒙项目源码、MySQL 数据库脚本（.sql）、项目运行说明（含各模块功能简介与运行截图）。
3. **项目交付过程文档**：需求分析文档（本文档）、数据库设计文档、进度安排分工表、接口文档、Git 协作开发规范、README 等。
4. **过程材料**：每日小组日报、代码作业提交记录。
5. **答辩演示**：完整可运行项目现场演示挂号全流程（选科室 → 选医生 → 选排班 → 确认挂号 → 查看记录 → 取消挂号）。

### 6.3 验收标准

- 功能验收：S1–S12 全部用户故事的验收标准逐条通过；挂号全流程（鸿蒙端 ↔ 后端）真联通。
- 防护验收：并发重复挂号被拦截；每分钟第 6 次挂号请求被限流；科室二次查询命中缓存。
- 数据验收：取消挂号后号源恢复 +1；后台修改科室后前台数据即时更新。
- 过程验收：Git 提交记录完整、规范；交付物齐全；每位成员均具备完整可运行的项目代码并参与答辩。

---

## 7. 范围界定与扩展专题

以下内容为实训教学日历中安排的**技术扩展专题**，**不属于 S1–S12 用户故事功能范围**，不计入本期功能验收；若正式纳入需求，须补充相应需求条目：

| 扩展专题 | 内容 | 说明 |
| --- | --- | --- |
| AI 导诊 | 基于 Spring AI 整合大模型（DeepSeek / 阿里百炼 DashScope），后端开发 AI 导诊接口并支持流式输出（SSE），鸿蒙端接入"AI 管家-小医"对话 | 第 7 天教学专题 |
| 消息队列 | 引入 RocketMQ 5.3.0，讲解异步消息解耦在挂号通知等场景的应用 | 第 8 天教学专题 |
| 管理端排班管理 | Vue 管理端对医生排班数据的维护界面 | 过程文档教学安排项，无对应用户故事 |
| 高并发进阶 | JMeter 压测、集群负载均衡、CDN、nginx+lua 限流、`@RateLimit` 注解 + AOP、布隆过滤器防穿透 | 教学演示与加分项 |

---

## 附录 A · 术语表

| 术语 | 解释 |
| --- | --- |
| JWT | JSON Web Token，无状态登录令牌 |
| 号源 | 某医生某时段可预约的挂号名额 |
| 排班 | 医生在具体日期、时段的出诊安排 |
| 就诊人 | 患者账号下代挂号的家庭成员（患者档案） |
| 订单号 | 挂号成功后生成的唯一凭证编号（时间戳 + 随机数） |
| Cache-Aside | 旁路缓存模式：先查缓存，未命中查库后回填；写操作时删缓存 |
| SETNX | Redis"不存在才设值"命令，分布式锁的实现基础 |
| 分布式锁 | 跨进程互斥机制，用于防止并发重复挂号 |
| 限流 | 对单位时间内的请求次数设上限，保护接口 |
| 缓存穿透/雪崩/击穿 | 缓存三大典型问题：查询不存在数据打穿缓存 / 大批 key 同时过期 / 热 key 过期瞬间高并发 |
| ArkTS / ArkUI | 鸿蒙应用开发语言与 UI 框架 |
| API 13 | HarmonyOS 5.0.1 对应的应用编程接口版本 |
| Codeup | 阿里云效代码托管与项目管理平台 |
| Apifox / Postman | 接口调试与联调工具 |
| SSE | Server-Sent Events，服务端流式推送技术（AI 导诊扩展专题使用） |
| RocketMQ | 阿里开源分布式消息队列（扩展专题使用） |
