# 成员C（前端 · Vue3 管理端） · 第 8 天：排班管理页 + 健壮性回归

| 项 | 内容 |
| --- | --- |
| 日期 | 2026-09-16（周三） |
| 关联故事 | 全局 / 迭代 M9 排班管理 |
| 当日主题 | 排班管理页（M9）+ 健壮性回归 |
| 契约依据 | 《项目拆解设计说明书》第 4/6/7 章 · db/init.sql · db/mock-data.md |

## 1. 当日目标

排班管理页（M9）上线；异常场景全覆盖。

## 2. 前置条件（开工前逐项确认）

- 无特殊前置，按计划开工

### 设计稿先行（页面任务动手前必看——先看稿、后写码）

> 回归核对以设计稿截图为唯一视觉基准，禁止凭记忆判断页面形态是否正确。

- 必读规范：doc/ui/UI统一规范.md
- 截图目录：doc/ui/pen/页面截图/（截图即设计稿，以图片为准）
- ℹ️ 本日为 M9 开发 + 全页面健壮性回归：回归以 M1–M4 全部截图为对照基准，重点核对 401 与异常反馈两态（下方两张）。
- ⚠️ M9 排班管理暂无截图：以设计稿 V1.4「M9 排班管理页」线框+字段规格为准开发，完成后回补截图。

本日重点对照截图：

- M4 全局-401提示页.png
- M4 全局-反馈状态.png

## 3. 任务清单（按序执行）

1. ⭐ 排班管理页（M9，/admin/schedules，侧边栏第六项）：①`src/api/schedule.ts` 定义 GET/POST/PUT/DELETE 四接口（类型从 mock-data.md V2.5 逐字段抄写）；②搜索区（医生下拉=科室过滤子科室→医生列表、日期区间默认本周）+「新增排班」按钮；③表格（时段 el-tag、已满 leftQuota danger、未来日期行才显示 改号源/删除 操作，过期行「—」）；④新增 Dialog 四字段（医生/日期禁选过去/时段单选/号源数 el-input-number 1–200）、编辑仅放开号源数、删除二次确认；⑤错误提示逐字对齐契约：5001/5002/5003/5004；mock 期用 mock-data.md V2.5 示例值，B·D05 接口就绪后切真实并填第 10 节对照表
2. 停服/超时 UI 兜底提示；HTTP 401 统一跳转验证
3. Chrome / Edge 双浏览器过一遍全部页面（含 M5–M9 六个迭代页）

## 4. 涉及契约（表 / 接口 / Key）

- 排班管理：拆解文档 4.6.4 · dto-contract V1.5 · mock-data V2.5（B·D05 实现）
- 401 行为：评审会 #9 结论

### 当日 DTO 速览（权威定义：db/dto-contract.md，与本表同源生成；冲突以契约文件为准并提对齐，禁止私自加改字段）

**GET /admin/schedules（M9） · AdminScheduleRespDto（响应，List）**：`id, doctorId, doctorName, deptName, workDate, timeSegment, totalQuota, leftQuota`

**POST /admin/schedules · AdminScheduleSaveReqDto（请求）**：`doctorId(@NotNull), workDate(仅当天及以后), timeSegment(上午/下午), totalQuota(@Min(1)≤200)`；错误：5001 唯一冲突 / 400 过去日期

**PUT /admin/schedules/{id} · AdminScheduleUpdateReqDto（请求）**：`totalQuota`（≥已挂号数否则 5004；仅未来日期否则 5002）

**DELETE /admin/schedules/{id}**：逻辑删除；有效挂号（待就诊）→5003；仅未来日期→5002

## 5. 自测清单（DoD，完成打勾）

- [ ] M9 CRUD 全流程 mock/真实通过；5001–5004 错误提示与契约逐字一致
- [ ] 断网/停服场景提示友好；双浏览器无样式错乱
- [ ] 全部九页（M2–M9 + 大屏）回归通过

## 6. 产出物

- 排班管理页（M9）+ 回归记录

## 7. 今日对齐点（涉及契约必读 AGENTS.md 铁律）

- 与 A 确认 401 行为（仅未登录返 HTTP 401）

## 8. 契约变更记录（涉 DDL / 接口结构 / 接口路径时必填，先对齐再动手）

| # | 时间 | 变更内容（DDL/接口结构/路径） | 影响成员 | 对齐状态 |
| --- | --- | --- | --- | --- |
| 1 | 2026-09-10 | 新增排班管理接口组（M9，铁律二/三）：/admin/schedules CRUD + AdminScheduleSaveReqDto/UpdateReqDto、错误码 5001–5004；「排班管理」由范围外转正（组长决策） | B（实现） | ☑ 已对齐（组长 2026-09-10 确认；dto-contract V1.5 / mock-data V2.5 / 拆解文档 4.6.4 已落稿） |

## 9. 答辩积累区（每日收工前必填，AGENTS.md 规则六/七；第 9 天汇总为答辩讲稿与问题库素材）

### a. 今日知识卡片（当日结束时你应能脱稿讲清以下点；「我的理解」必须自己写，不许 AI 代写）

| 技术点（当日预填） | 我的理解（自己写一两句） | 对应代码/文件 | 预判老师追问 |
| --- | --- | --- | --- |
| HTTP 401 与业务码的处理分工 |  |  |  |

### b. 今日最有讲头的问题（从 exception-day08/ 的 issue 中挑 1 条，浓缩成一句话答辩素材；无 issue 则填「今日无」）

| 问题一句话 | 怎么定位 | 怎么解决 | issue 文件名 |
| --- | --- | --- | --- |
|  |  |  |  |

### c. 素材归档

今日演示级素材（截图/录屏/数据）已存 doc/screenshots/，文件名：　　　（无则填「无」）

### d. 复述自检（规则六）

- [ ] 已把今日核心改动讲给至少一位组员听（2 分钟版），对方表示听懂

## 10. 接口一致性对照表（当日有接口编码/对接必填，AGENTS.md 规则八三步绑定；无则写「当日不涉及接口编码」）

| 接口（方法+路径） | 我方端 | 契约出处 | 编码前契约复述 | 编码后逐字段对照 | 不一致→处理（缺陷清单/铁律） |
| --- | --- | --- | --- | --- | --- |
|  | FE / BE | 拆解文档 4.x / mock §x | ☐ | ☐ |  |

> 填写规则（规则八）：每实现/对接一个接口记一行——编码前先向 AI 核对契约原文（第 1 步），编码后逐字段对照并打勾（第 3 步）；发现不一致走第 8 节契约变更记录或缺陷清单，**严禁私自做字段映射适配**。

---

[← 上一份](D07-扩展-AI对话页(可选).md) · [目录规则](AGENTS.md) · [下一份 →](D09-运行说明与PPT.md)
