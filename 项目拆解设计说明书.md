# 东软云医院挂号预约系统 · 项目拆解设计说明书

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.9 |
| 编写日期 | 2026-09-08（V1.6 DDL 安全审查修复；V1.7 挂号费口径闭环；V1.8 delmark 六表补齐 V2.2；V1.9 需求迭代·挂号流程改版契约变更） |
| 适用对象 | 开发 / 测试 / 前端（Vue、鸿蒙）联调成员 |
| 文档密级 | 项目内部 |
| 维护人 | 第14组（张家俊组） |
| 上游文档 | 《东软云医院挂号预约系统 · 需求分析说明书》V1.1（用户故事 S1–S12） |
| 本文档职责 | 将 S1–S12 拆解为：架构演进路线、Maven 模块与 DDD 分包、接口契约（含待对齐项）、统一响应体与全局异常、数据库设计、中间件接入入口、四人分工与十天计划 |

> **修订记录**：V1.1 —— 新增 1.4「技术栈基线（开发硬性前提）」；ORM 确定沿用过程文档统一方案 **MyBatis-Flex 1.8.8**（曾评估 MyBatis-Plus，最终为与实训统一技术方案、过程性考核保持一致而定 Flex）；4.8 待对齐项 #6（密码加密）定为 BCrypt（Hutool）。
> **修订记录**：V1.2 —— 第 9 章概要矩阵细化为独立文档**《任务分工说明书》**（每人按天的完整任务、验收标准、依赖关系与签字认领表），本文件保留概览与追溯职责。
> **修订记录**：V1.3 —— DDL 定稿并独立为 `db/init.sql`（含种子数据）+ `db/mock-data.md`（前端 mock 数据契约）；4.8 待对齐 #5（职称编码）定为 TINYINT 0/1/2，dept_category 取值与 gender 编码同步定稿；本文档随三份总体文档迁入项目根目录 `neusoft-his/`，逐天工作计划见 `member-a/b/c/d/` 子目录（每人 10 份，含 AGENTS.md 对齐规则）。
> **修订记录**：V1.4 —— **数据库契约整体切换为 `cloud_hospital.sql` 定稿版（V2.0 契约，全组已对齐）**：库名 neusoft_hospital；六表更名（patient_user / dept / doctor / schedule / patient_member / appointment_record）；逻辑删除字段统一为 `delmark`（schedule 表无此字段，既定设计）；职称/时段/性别/状态改为**中文字符串直传**（title / time_segment / gender / status），前端零映射；排班字段更名（work_date / time_segment / left_quota）；挂号记录仅存名称快照。第 4 章接口 DTO、6.1–6.5、7.1 Redis key、db/init.sql、db/mock-data.md、40 份日计划已同步；设计取舍与实现约定见新增 **6.5 节**。
> **修订记录**：V1.5 —— 新增 **db/dto-contract.md（全量 DTO 契约）**：15 个 ReqDto/RespDto/通用包装的字段、类型、必填、校验与 DDL 列映射，由 tools/gen-day-plans.mjs 与 40 份日计划的"当日 DTO 速览"**同源生成**（单一来源，重跑即同步，防前后端漂移）；4.1 增加 DTO 结构条目；硬性契约由三份扩为四份（铁律二同步更新）。
> **修订记录**：V1.6 —— **DDL 安全审查修复（db/init.sql V2.1，接口契约零变化）**：① schedule 增加 `uk_doctor_date_segment` 唯一键（堵"重复排班→扣号错行/超卖"漏洞）；② appointment_record 新增 `doctor_id / schedule_id` 列（取消挂号按 schedule_id 直达回补，不再依赖姓名快照反查，医生改名不断链；"医生姓名全库唯一"降级为展示建议）；③ title/time_segment/gender/status 增加 CHECK 枚举、left_quota 增加 ≥0 且 ≤total_quota 约束（MySQL 8.0.16+ 生效）；④ 新口径：删医生同事务物理删其未来排班（防孤儿排班）、挂号费=职称映射（主任 30/副主任 20/主治 15，6.5-⑥）、appointment_record 无 delmark（订单永久保留，6.5-⑤）；⑤ 修正 6.4：BaseEntity(delmark) 仅四表继承。6.5 扩充为八条实现约定。
> **修订记录**：V1.7 —— **挂号费口径闭环（铁律二变更：DoctorRespDto 新增 `registerFee` 字段）**：定价模型定为**按职称统一映射**（主任医师 30 / 副主任医师 20 / 主治医师 15，常量定义于 his-common，不加库字段、不加管理端表单项）；`GET /doctors`（及管理端医生列表）由后端计算并返回 registerFee，**确认挂号页的费用展示来源即此字段**，前端禁止自行维护价格映射；提交挂号时请求体仍只有 scheduleId/memberId（**不传费用**），后端下单时重算并落库为快照 register_fee（防篡改）。db/dto-contract.md V1.2、db/mock-data.md V2.2 同步。
> **修订记录**：V1.8 —— **delmark 六表补齐（db/init.sql V2.2，接口契约零变化）**：schedule / appointment_record 补上逻辑删除字段——逻辑删除是防数据丢失的统一兜底手段，**"不删除"的业务限制由后端代码层实现（订单不提供删除接口、排班过期靠时间窗过滤），而不是靠表结构缺字段**；连带优化：删除医生时对未来排班的处理由"物理删除"改为"逻辑删除 delmark=1"，全库删除口径统一；6.4 BaseEntity 恢复六表统一继承（不再分档）。
> **修订记录**：V1.9 —— **需求迭代·挂号流程改版（init.sql V2.3 / dto-contract V1.3 / mock-data V2.3 同步）**：①首页改双入口（当天/预约挂号），科室数据后移至流程内 P4；②dept 新增 `parent_id` 父子两级分类（父分类七类种子 id 5–11，原子科室 id 1–4 挂接；GET /departments 返回两级平铺，`DepartmentSaveReqDto` 改传 parentId，删除父分类需校验无子科室）；③新增 `GET /quotas?deptId=&date=`（QuotaRespDto：按科室+日期查医生号源，后端排序上午前下午后、职称主任→副主任→主治）；④新增 `GET /quotas/days?deptId=`（QuotaDayRespDto：日期条三态 可约/已满/休）；⑤`GET /schedules` 新增 deptId 入参、ScheduleRespDto 增 doctorId/doctorName/title（P5 科室级排班总览，窗口定为当天~+6）；⑥排班种子窗口扩为当天~+7 并差异化余号/休息日（覆盖 充足/紧张/已满/休 四档演示）；待对齐 #4（科室分类筛选）随之关闭。
> **修订记录**：V1.10 —— **需求迭代·统计大屏契约定稿（dto-contract V1.4 / mock-data V2.4 同步，组长 2026-09-10 确认）**：①新增 **4.6.1 统计大屏接口组**——`GET /admin/stats/summary、/trend?days=7、/dept-rank、/doctor-top?limit=5、/title-ratio、/latest?limit=4` **按卡拆分 6 接口**（待对齐 #13 定稿）+ 6 个 Stats*RespDto，后端 B 实现（排期 B·D04）、管理端 C 消费（大屏页排期 C·D04，联调切换 C·D05）；②统计口径定稿（#15）：趋势/排行/占比=近 7 天（含当天）有效挂号单（不含已取消）、KPI 环比较昨日、注册用户较上周，`days`/`limit` 不放开；③图表库定稿（#14）：ECharts 5 + vue-echarts（组长评审通过）；④4.1 路径清单补 `/quotas`（V1.9 漏登）与 `/admin/stats`；⑤实现排期补齐：`/quotas`、`/quotas/days` 归 B·D03（schedule 查询域），`GET /schedules` 按 V1.9 定稿口径实现。
> **修订记录**：V1.11 —— **需求迭代·管理端运营页（init.sql V2.4 / dto-contract V1.5 / mock-data V2.5 同步，组长 2026-09-10 确认）**：①管理端新增三页——**M7 患者管理**（GET /admin/users 分页+keyword、POST /admin/users/{id}/ban、/unban 封禁/解封，his-user·B·D05）；**M8 挂号单管理**（GET /admin/registrations 只读分页+date/status/keyword 筛选，his-registration·A·D05——挂号域只读列表归 A，不做管理端代取消）；**M9 排班管理**（/admin/schedules 列表+POST+PUT+DELETE 基础 CRUD，仅未来日期可改/删、唯一键冲突 5001、已有有效挂号不可删 5003，his-hospital·B·D05；Excel 批量导入列为扩展可选）；②**铁律一 DDL 变更**：patient_user 新增 `status` 列（正常/已封禁，CHECK 枚举，默认正常）→ 封禁账号登录返回新错误码 **1003**；③错误码表新增 1003、5001–5004（5xxx=管理端运营域）；④「排班管理」由风险表原「范围外」转正纳入本期（组长决策）；⑤管理端侧边栏由三项扩为六项（M1·V1.4）。
> **修订记录**：V1.12 —— **需求迭代·站内信+MQ（迁移脚本 V2.5 / dto-contract V1.6 / mock-data V2.6 同步，组长 2026-09-10 确认）**：①**站内信机制定稿**——新增 `site_message` 表（迁移脚本 `db/migrations/V2.5__create_site_message.sql`，init.sql V2.4 基线不动）+ 新增 **4.9 站内信接口组**（GET /messages 分页、GET /messages/unread-count、POST /messages/{id}/read、POST /messages/read-all、管理端群发 POST /admin/messages，错误码新增 **4004**）；触发场景定稿=**挂号成功 + 挂号已取消 + 管理端群发系统公告**；②**MQ 由 7.3「范围外」转正**——统一通知 topic 定为 `his_notification`（替代原 his_registration_success），消息体 NotificationMessage（type/orderNo/userId/memberName/doctorName/workDate/timeSegment），producer=his-registration（挂号成功/取消事务提交后发送），consumer=his-message 模块（落库 site_message）；③rocketmq-spring-boot-starter 定稿 **2.3.1**（1.4 去【待对齐】），本地环境 Docker 跑 NameServer+Broker；`his.mq.enabled` 默认 false 走 LoggingMessageSender 降级（沿用 2.4 port 双实现）；④实现归属：**后端 A 全链路**（his-message 模块 + MQ 生产/消费，排期 A·D08）；迁移脚本 V2.5 由 B 终审（排期 B·D07）；前端 D·D07 消息中心页 P11 + 首页铃铛（AI 管家保持可选）、C·D09 公告群发页 M10。

---

## 1. 拆解说明

### 1.1 拆解目标

针对四人小组、零基础 Java 的情况，把《需求分析说明书》中的 S1–S12 拆成**可并行认领、可逐天验收**的工程任务，并给出以下五份"契约"，使前后端可以**接口先行、并行开发**：

1. **架构契约**：从单模块单体 CRUD 起步 → Maven 多模块 DDD 拆包 → Redis / MQ 中间件接入的演进路线；
2. **接口契约**：统一接口路径 + 每个故事对应的接口清单（请求/响应初拟，含【待对齐】标记）；
3. **数据契约**：六张表的建表约定（公共字段 `delmark` / `create_time` / `update_time` **六表齐备**；"不删除"的限制由后端代码实现而非省略字段，见 6.5-⑤）；
4. **代码契约**：统一响应体、全局异常处理器、DTO 命名与封装规则；
5. **协作契约**：四人分工矩阵、十天任务矩阵、里程碑验收清单。

### 1.2 拆解原则

| 原则 | 说明 |
| --- | --- |
| 从简单到完整 | 先跑通纯 CRUD 单体，再拆多模块，最后接中间件；每一步都可运行、可演示、可回退 |
| 包即模块边界 | 单模块阶段就按未来多模块的包结构分包，后期拆分只是"包 → 模块"的机械迁移，不改业务代码 |
| 接口先行 | 本文档第 4 章即前后端联调契约；所有参数均为**初拟**，未确认处以【待对齐】标出，第 2 天接口评审会一次性对齐 |
| 依赖倒置留入口 | Redis / MQ 不第一天引入，但通过 4 个 port 接口（缓存/锁/限流/消息）+ 配置开关留好接入入口，业务代码面向接口编程，接入时**零改动** |
| 约定大于配置 | 统一响应体、统一异常、统一公共字段、统一 DTO 后缀，减少零基础成员的决策成本 |

### 1.3 与需求文档的追溯关系

S1–S12 → 领域（第 3 章）→ Maven 模块（第 2 章）→ 接口（第 4 章）→ 数据表（第 6 章）→ 分工与排期（第 9 章），一一对应，追溯矩阵见附录 A。

### 1.4 技术栈基线（开发硬性前提）

> 本节为全组统一版本基线：**任何人不得私自升降版本或引入同功能替代组件**，新增第三方依赖须经组长评审并更新本表。"Spring 3" 统一理解为 **Spring Boot 3.x**，定版 **3.1.5**（与教学环境/过程文档一致，不追新）；JDK 定 **17**（LTS，Boot 3 最低要求，教学环境同版，不用 21）。
>
> ✅ **与过程文档一致**：ORM 沿用实训统一方案的 **MyBatis-Flex 1.8.8**（曾评估 MyBatis-Plus，最终为与统一技术方案及过程性考核保持一致而定 Flex），无技术偏差，无需向带队老师报备。

#### 1.4.1 版本清单（后端）

| 层 | 组件 | 坐标（groupId:artifactId） | 版本 | 说明 |
| --- | --- | --- | --- | --- |
| 运行时 | JDK | — | **17**（LTS） | Boot 3 最低要求；安装路径无中文/空格 |
| 构建 | Maven | — | **3.9.11+** | settings.xml 配阿里云镜像 |
| 核心框架 | Spring Boot | `org.springframework.boot:spring-boot-starter-parent` | **3.1.5** | Spring Framework 6.x / Jakarta EE 命名空间 |
| ORM | MyBatis-Flex | `com.mybatis-flex:mybatis-flex-spring-boot3-starter` | **1.8.8** | 与过程文档统一方案一致；⚠️ 必须 boot3 专用 starter；分页/逻辑删除内置 |
| 数据库 | MySQL 服务端 | — | **8.0** | 六表见第 6 章 |
| 数据库驱动 | mysql-connector-j | `com.mysql:mysql-connector-j` | Boot 托管（不写版本号） | ⚠️ 不用旧 `mysql-connector-java 5.1.x` |
| 连接池 | HikariCP | Boot 默认 | Boot 托管 | 零配置，不引入 Druid |
| 参数校验 | Validation | `spring-boot-starter-validation` | Boot 托管 | ⚠️ Boot 3 必须显式引入（hibernate-validator） |
| 接口文档 | Knife4j | `com.github.xiaoymin:knife4j-openapi3-jakarta-spring-boot-starter` | **4.4.0** | ⚠️ 必须用 **jakarta** 后缀版本；/doc.html |
| 认证 | java-jwt | `com.auth0:java-jwt` | **4.4.0** | JWT 签发/校验，有效期 24h |
| 工具库 | Hutool | `cn.hutool:hutool-all` | **5.8.32**（或以上 5.8.x） | `IdcardUtil`（S7 身份证校验）、`BCrypt`（密码加密）、StrUtil/CollUtil |
| 简化开发 | Lombok | `org.projectlombok:lombok` | Boot 托管 | IDEA 装插件并开启注解处理（Enable annotation processing） |
| 缓存客户端 | Redis | `org.springframework.boot:spring-boot-starter-data-redis` | Boot 托管（Lettuce） | 服务端教学环境 Redis 7.2.x；本机 Windows Redis 6.x 可练手（SETNX/INCR 命令一致） |
| MQ（扩展→V1.12 转正） | RocketMQ | `org.apache.rocketmq:rocketmq-spring-boot-starter` | 2.3.1（定稿，适配 SB3） | 站内信异步通知（7.3，A·D08） |
| 监控（可选） | Actuator | `spring-boot-starter-actuator` | Boot 托管 | /actuator/health 健康检查，演示用 |

**前端与联调（维持《需求分析说明书》1.5，不另行选型）**：Vue3 + Element Plus + Vite + Vue Router + Pinia（Node.js 18+）；HarmonyOS 5.0.1（API 13）+ DevEco Studio 6.1.0 + @ohos/axios 2.2.10；Apifox 联调、JMeter 5.6（第 9 天可选压测）、Another Redis Desktop Manager（本机可视化）。

#### 1.4.2 父 pom 版本锁定（properties 片段）

```xml
<properties>
    <java.version>17</java.version>
    <mybatis-flex.version>1.8.8</mybatis-flex.version>
    <knife4j.version>4.4.0</knife4j.version>
    <java-jwt.version>4.4.0</java-jwt.version>
    <hutool.version>5.8.32</hutool.version>
</properties>
```

> 原则：凡能被 `spring-boot-starter-parent` 托管的一律**不写版本号**（MySQL 驱动、Redis、Lombok、Validation、HikariCP、Jackson、Actuator）；只有 Boot BOM 不管的才在父 pom 定版。

#### 1.4.3 Spring Boot 3 + MyBatis-Flex 六个坑（零基础必读）

1. **boot3 系 starter**：`com.mybatis-flex:mybatis-flex-spring-boot3-starter`（不是 boot2 用的 `mybatis-flex-spring-boot-starter`）；Knife4j 用 jakarta 版；所有 `javax.*` 导入改 `jakarta.*`（如 `jakarta.validation.constraints.NotBlank`）；
2. **分页无需配插件**：Flex 分页是内置能力（`mapper.paginate(...)`），照抄 MyBatis-Plus 的分页拦截器教程反而画蛇添足；返回 Flex 的 `Page<T>`，由 Service 统一转 5.1 节 `PageDTO`；
3. **逻辑删除**：实体 `delmark` 字段标 `@Column(isLogicDelete = true)`，默认值（0-正常 / 1-删除）与 DDL 一致：delete 自动改写为 `UPDATE ... SET delmark='1'`，查询自动追加 `delmark='0'`，无需手工拼条件（六表统一 delmark，见 6.4）；
4. **公共时间字段**：`create_time/update_time` 走数据库默认值方案（见 6.1），实体字段保持 null 即不进 SQL（Flex 插入/更新默认忽略 null 字段），**不引入** InsertListener/UpdateListener 自动填充做双轨；
5. **Redis 配置前缀是 `spring.data.redis`**（Boot 2 旧博客写 `spring.redis`，照抄必踩坑）；
6. **MyBatis-Flex 使用边界**：只用 `BaseMapper + QueryWrapper / UpdateChain + paginate`；复杂 SQL 才写 XML（放各模块 `resources/mapper/`，默认扫描路径已覆盖）；不使用 Db+Row 动态表、多租户、数据脱敏、乐观锁等高级特性，保持 Controller→Service→Mapper 三层。

---

## 2. 总体架构与演进路线

### 2.1 三阶段演进总览

| 阶段 | 时间 | 形态 | 内容 | 退出标准（验收） |
| --- | --- | --- | --- | --- |
| 阶段一 · 单模块单体 | 第 1–2 天 | Maven **单模块** `his-api`，纯 CRUD | 骨架 + 统一响应体/异常 + 六表 + S1/S2 + S4/S5 查询；包按三域划分；中间件 port 接口就位（Noop 实现） | `/doc.html` 可访问；S1/S2 接口 Apifox 走通 |
| 阶段二 · 多模块 DDD | 第 2 天末拆分，第 3 天起 | Maven **多模块**（1 父 + 5 子） | 包 → 模块机械拆分；S3–S12 全部业务在多模块下开发 | 拆分后全量接口回归通过 |
| 阶段三 · 中间件接入 | 第 4 天起 | 多模块 + Redis（第 8 天扩展 MQ） | Redis 缓存（S4）、分布式锁 + 限流（S8）；RocketMQ 挂号通知（扩展专题） | 缓存命中可见；并发重复挂号被拦截；限流生效 |

> 说明：一开始用单模块单体跑通简单 CRUD 是为了降低零基础起步成本；但包结构从第一天起就等于未来的模块边界，因此"拆模块"不重构业务逻辑，只是移动代码 + 补 pom，成本控制在半天以内。

### 2.2 阶段一：单模块单体（第 1–2 天）

```
his-api/                                    # Maven 单模块工程（SpringBoot 3.1.5 / JDK 17）
└── src/main/
    ├── java/com/neusoft/his/
    │   ├── HisApplication.java             # 启动类
    │   ├── common/                         # ★ 公共支撑（未来的 his-common 模块）
    │   │   ├── result/                     #    统一响应体 Result<T>、PageDTO
    │   │   ├── exception/                  #    BizException、ErrorCode、GlobalExceptionHandler
    │   │   ├── constants/                  #    RedisKeyConstants、CommonConstants
    │   │   ├── enums/                      #    职称/时段/状态/性别枚举
    │   │   ├── port/                       #    ★ 中间件接入入口（4 个接口，见 2.4）
    │   │   └── util/                       #    JwtUtil、OrderIdUtil、IdCardUtil
    │   ├── user/                           # ★ 用户与认证域（未来的 his-user 模块）
    │   │   ├── controller/  service/  mapper/  entity/  dto/
    │   ├── hospital/                       # ★ 医院资源域（未来的 his-hospital 模块）
    │   │   ├── controller/  service/  mapper/  entity/  dto/
    │   └── registration/                   # ★ 挂号域（未来的 his-registration 模块）
    │       ├── controller/  service/  mapper/  entity/  dto/
    └── resources/
        ├── application.yml                 # 端口 8080、context-path /api、数据源
        └── application-middleware.yml      # ★ Redis / MQ 配置预留段（默认注释，见 2.4.3）
```

### 2.3 阶段二：多模块 DDD 拆分（终态结构，第 2 天末完成）

```
neusoft-his/                                # 父工程（packaging=pom，统一管版本）
├── pom.xml                                 # spring-boot-starter-parent 3.1.5 + dependencyManagement
├── his-common/                             # 公共模块：Result / 异常 / 枚举 / 常量 / 工具 / port 接口
├── his-user/                               # 用户与认证域：S1 S2 S3 S7
├── his-hospital/                           # 医院资源域：S4 S5 S6 S11 S12
├── his-registration/                       # 挂号域：S8 S9 S10
└── his-api/                                # 启动与装配模块（唯一可运行，打 jar）
    └── src/main/
        ├── java/com/neusoft/his/HisApplication.java   # scanBasePackages = "com.neusoft.his"
        │                                              # + config/（Knife4j、拦截器、Redis 配置占位）
        └── resources/application.yml                  # 端口 8080、context-path /api
```

**模块依赖规则（保持单向无环）：**

```
his-api ──→ his-registration ──→ his-hospital ──→ his-common
   │                │                              
   ├──→ his-user ───┘              规则：user 与 hospital 互不依赖；
   └──→ his-hospital                  只有 registration 可依赖另外两个业务模块
```

**DDD 概念映射表（答辩用）：**

| DDD 概念 | 本项目落位 | 说明 |
| --- | --- | --- |
| 限界上下文 | his-user / his-hospital / his-registration 三个业务模块 | 按业务能力划分，模块即边界 |
| 接口层（适配器） | `controller` 包 | 面向前端，只做参数校验与编排调用 |
| 应用层 | `service` 包 | 用例编排（如挂号 = 限流→锁→校验→扣号→落库→发通知） |
| 领域层 | `entity` 包 + 实体内业务方法 | 零基础阶段先贫血模型，排班扣减号源等规则逐步下沉到实体方法（演进方向） |
| 基础设施层 | `mapper` 包 + port 的中间件实现类 | MyBatis-Flex 持久化、Redis/RocketMQ 适配 |
| 端口适配器（防腐） | `common/port` 四接口 + Noop/Redis 双实现 | 业务面向端口，中间件可插拔 |

**拆分操作清单（机械迁移，半天内完成）：**

1. 父工程 `neusoft-his` 建好，声明 `spring-boot-starter-parent 3.1.5` 与 `dependencyManagement`（版本一律以 **1.4 节技术栈基线** 为准：mybatis-flex-spring-boot3-starter、java-jwt、knife4j jakarta、hutool 等）；
2. 建 5 个子模块，`his-common` 装入原 `common` 包，三个业务模块按原包整体迁移（含 mapper 扫描路径确认）；
3. `his-api` 只留启动类 + 全部配置 + `Web/拦截器/Knife4j` 配置类；
4. 业务模块 pom 只依赖 `his-common`（registration 追加 user、hospital）；`spring-boot-maven-plugin` 只放 `his-api`；
5. 启动类加 `@MapperScan("com.neusoft.his.**.mapper")`，全量接口回归（Apifox 全部用例重跑）。

### 2.4 阶段三：中间件接入入口（关键设计）

#### 2.4.1 四个 port 接口（位于 `his-common/port`）

```java
/** S4 科室缓存入口：阶段一 Noop 实现（恒回源），阶段三 Redis Cache-Aside 实现 */
public interface CacheService {
    <T> T get(String key, Class<T> type);
    void set(String key, Object value, long ttlSeconds);
    void evictByPrefix(String prefix);        // 管理端"写时删缓存"（S11/S12）
}

/** S8 防重复挂号入口：阶段一 Noop（直接放行/本地锁），阶段三 Redis SETNX 实现 */
public interface DistributedLockService {
    boolean tryLock(String key, long expireSeconds);
    void unlock(String key);                  // 必须在 finally 中调用
}

/** S8 限流入口：阶段一 Noop（恒通过），阶段三 Redis 计数器实现 */
public interface RateLimitService {
    boolean allow(String key, int limit, int windowSeconds);
}

/** MQ 入口（挂号成功通知，第 8 天扩展）：阶段一日志实现，扩展期 RocketMQ 实现 */
public interface MessageSender {
    <T> void send(String topic, T message);
}
```

#### 2.4.2 实现切换：配置开关，不改业务代码

| 接口 | 阶段一实现（默认） | 阶段三实现（打开开关后生效） |
| --- | --- | --- |
| CacheService | `NoopCacheService`（get 恒 null，恒回源） | `RedisCacheService`（StringRedisTemplate + JSON，TTL 1800s + 随机抖动） |
| DistributedLockService | `NoopLockService`（tryLock 恒 true） | `RedisSetnxLockService`（setIfAbsent，30s 兜底过期） |
| RateLimitService | `NoopRateLimitService`（恒 true） | `RedisCounterRateLimitService`（INCR + EXPIRE） |
| MessageSender | `LoggingMessageSender`（打日志，便于联调观察） | `RocketMQMessageSender`（rocketmq-spring-boot-starter） |

Redis 实现类统一标注 `@ConditionalOnProperty(name = "his.middleware.redis.enabled", havingValue = "true")`，Noop 实现标注 `matchIfMissing = true`。即 **yml 一个布尔值切换整套中间件**，机房 Redis 故障时可降级演示。

#### 2.4.3 依赖与配置预留（第一天就写好，默认关闭）

```xml
<!-- his-api/pom.xml：中间件依赖预留（阶段三去掉注释即接入；版本由父 pom 统一管理） -->
<!--
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>
<dependency>
    <groupId>org.apache.rocketmq</groupId>
    <artifactId>rocketmq-spring-boot-starter</artifactId>
</dependency>
-->
```

```yaml
# application.yml：中间件配置预留（阶段三打开）
his:
  middleware:
    redis:
      enabled: false        # 阶段三置 true，Cache/Lock/RateLimit 自动切为 Redis 实现
  jwt:
    secret: neusoft-his-jwt-secret-2026
    expire-hours: 24
# spring:
#   data:
#     redis:                # 阶段三启用（Redis 7.2.x）
#       host: 127.0.0.1
#       port: 6379
```

---

## 3. 领域（模块）拆解

### 3.1 限界上下文划分

| 领域（上下文） | 模块 | 承接故事 | 核心职责 | 聚合实体（表） |
| --- | --- | --- | --- | --- |
| 用户与认证域 | his-user | S1 S2 S3 S7 | 注册/登录/退出（JWT 签发校验）、就诊人档案管理（≤3 人、身份证校验） | patient_user、patient_member |
| 医院资源域 | his-hospital | S4 S5 S6 S11 S12 | 科室/医生的 patient 端查询与 admin 端维护、排班与号源查询、号源扣减/回补的原子操作 | dept、doctor、schedule |
| 挂号域 | his-registration | S8 S9 S10 | 挂号下单编排（限流→锁→校验→扣号→落库→发通知）、记录查询、取消（事务） | appointment_record |
| 公共支撑 | his-common | 全部 | 响应体/异常/错误码/枚举/工具/port 中间件入口 | 无表 |

### 3.2 跨域协作规则

- 挂号下单时，`RegistrationService`（应用层编排）调用：`RateLimitService` → `DistributedLockService` → `MemberService`（校验就诊人归属，user 域）→ `ScheduleService`（校验并扣减号源，hospital 域）→ 落库 → `MessageSender.send(...)`；
- 模块间只允许 **service 层单向调用**，禁止跨模块访问对方 mapper；
- 单体多模块内直接方法调用即可（同 JVM）；未来若拆微服务，按此依赖方向改为 RPC/事件，代码结构不变。

---

## 4. 接口设计（接口先行 · 联调契约）

> **⚠️ 全章声明**：以下接口路径为**统一定稿**；所有请求/响应字段均为**初拟**，标注【待对齐】处须在第 2 天"接口评审会"上由前后端共同确认后更新本文档并同步 Apifox。未标注【待对齐】的字段如需变更，也必须在评审会提出，禁止口头改契约。

### 4.1 统一接口规范

| 项 | 约定 |
| --- | --- |
| 基础前缀 | `http://localhost:8080/api`（端口 8080 + context-path `/api`），下文路径均省略 `/api` |
| 患者端路径 | 资源复数名词：`/auth`、`/members`、`/departments`、`/doctors`、`/schedules`、`/quotas`（V1.10 补登，V1.9 新增）、`/registrations` |
| 管理端路径 | 统一加 `/admin` 前缀：`/admin/departments`、`/admin/doctors`、`/admin/stats`（V1.10 统计接口组，见 4.6.1）、`/admin/users`、`/admin/registrations`、`/admin/schedules`（V1.11 运营接口组，见 4.6.2–4.6.4），便于按前缀做角色拦截 |
| 方法语义 | 查询 GET、新增 POST、修改 PUT、删除 DELETE、业务动作 POST `.../{id}/动作`（如取消挂号） |
| 登录态 | 请求头 `Authorization: Bearer {token}`【待对齐：是否带 Bearer 前缀，与鸿蒙端 axios 封装统一】 |
| 分页参数 | `pageNum`（默认 1）/ `pageSize`（默认 10），响应用 `PageDTO<T>`【待对齐：字段结构见 5.1】 |
| 时间格式 | 日期 `yyyy-MM-dd`、时间 `yyyy-MM-dd HH:mm:ss` |
| DTO 结构 | 全量 ReqDto/RespDto 的字段、类型、必填、校验见 **db/dto-contract.md**（权威，脚本生成）；JSON 示例见 db/mock-data.md；40 份日计划内嵌当日 DTO 速览（同源） |
| 文档 | Knife4j（/doc.html）为准，Apifox 同步维护供前端 mock |

### 4.2 DTO 设计规约（全组强制）

1. **请求参数 > 3 个**：必须封装为独立请求类 `XxxReqDto`；≤ 3 个可用 `@RequestParam` / `@PathVariable` 直接接收，**边界情况（恰为 3 个）是否封装标【待对齐】**；
2. **响应字段 ≥ 2 个**：必须封装为 `XxxRespDto`；仅 1 个字段可直接返回简单类型（仍由 `Result<T>` 包装）；
3. 命名后缀统一：请求类以 **ReqDto** 结尾、响应类以 **RespDto** 结尾，放在所属域的 `dto` 包；
4. **Entity 一律不出网**：实体与 DTO 分离（防止密码等敏感字段泄露，登录响应不含密码）；
5. 参数校验用 `jakarta.validation` 注解（`@NotBlank`、`@Pattern` 等）+ Controller 上 `@Validated`；
6. 字段类型：日期 `LocalDate`、时间 `LocalDateTime`、金额 `BigDecimal`、状态用枚举或 Integer（配注释）。

### 4.3 用户与认证域接口（his-user）

| 故事 | 接口 | 请求（初拟） | 响应（初拟） | 错误码 |
| --- | --- | --- | --- | --- |
| S1 注册 | `POST /auth/register` | `RegisterReqDto{username, password, confirmPassword}`（3 个字段，边界【待对齐：封装/简单参数】） | `Result<Void>` | 1001 用户名已存在 |
| S2 登录 | `POST /auth/login` | `LoginReqDto{username, password}`（2 个字段【待对齐：封装/简单参数】） | `Result<LoginRespDto>` | 1002 用户名或密码错误 |
| S3 退出 | `POST /auth/logout` | Header 携带 Token，无 body | `Result<Void>`（服务端是否做 Token 失效【待对齐，扩展项】） | 401 未登录 |

```json
// POST /auth/login 响应示例（不含密码字段）
{ "code": 200, "message": "登录成功",
  "data": { "token": "eyJhbGciOi...", "userId": 1, "username": "zhangsan" } }
```

### 4.4 就诊人接口（his-user）

| 故事 | 接口 | 请求（初拟） | 响应（初拟） | 错误码 |
| --- | --- | --- | --- | --- |
| S7 添加就诊人 | `POST /members` | `MemberCreateReqDto{memberName, gender, idCard, phone, birthday}`（5 > 3，必须封装；gender 直传"男"/"女"；phone 必填——与表 NOT NULL 一致） | `Result<Void>` | 2001 超出 3 人上限；2002 身份证格式错误 |
| S7 就诊人列表 | `GET /members` | 无参（从 Token 取 userId） | `Result<List<MemberRespDto>>` | — |
| （扩展）删除就诊人 | `DELETE /members/{id}` | 路径参数【待对齐：需求未明确，是否纳入本期】 | `Result<Void>` | — |

`MemberRespDto`：`id, memberName, gender, idCard, phone, birthday`（≥ 2 字段，必须封装）。

### 4.5 医院资源域 · 患者端查询接口（his-hospital）

| 故事 | 接口 | 请求（初拟） | 响应（初拟） | 错误码 |
| --- | --- | --- | --- | --- |
| S4 科室列表 | `GET /departments` | 无参（V1.9：返回**父子两级平铺**——7 父分类节点 + 子科室，前端按 parentId 组两级；原 category 筛选参数废弃） | `Result<List<DepartmentRespDto>>`（含 parentId，见下方 DTO 说明） | — |
| S5 医生列表 | `GET /doctors` | `deptId`（必填，Long） | `Result<List<DoctorRespDto>>`（按职称排序：主任→副主任→主治，后端 Service 权重排序实现，见 6.4） | 400 参数错误 |
| 挂号 P4A/P4B 号源（V1.9 新增） | `GET /quotas` | `deptId`、`date`（均必填；当天挂号传当天、预约传所选日期，yyyy-MM-dd） | `Result<List<QuotaRespDto>>`（医生号源行；后端排序**先上午后下午、时段内职称主任→副主任→主治**） | 400 参数错误 |
| 挂号 P4B 日期条（V1.9 新增） | `GET /quotas/days` | `deptId`（必填） | `Result<List<QuotaDayRespDto>>`（当天+1~+7 共 7 天：date + dayStatus 可约/已满/休） | 400 参数错误 |
| S6 医生排班 / P5 排班总览 | `GET /schedules` | `doctorId` 与 `deptId` **二选一**（deptId 为 V1.9 新增，供 P5 科室级排班总览，二者都传以 doctorId 为准）；查询窗口固定**当天 ~ 当天+6**（日期入参不放开，日期维度由 /quotas 承担——V1.9 定稿） | `Result<List<ScheduleRespDto>>`（V1.9 起带 doctorId/doctorName/title，deptId 查询时前端按医生分组渲染；无排班返回空数组，前端渲染"暂无排班"） | — |

```json
// GET /schedules?doctorId=1 响应示例（timeSegment 中文直传；V1.9 起带医生字段）
{ "code": 200, "message": "成功",
  "data": [ { "id": 101, "doctorId": 1, "doctorName": "周建国", "title": "主任医师",
              "workDate": "2026-09-09", "timeSegment": "上午",
              "totalQuota": 30, "leftQuota": 5 } ] }
```

- `DepartmentRespDto`：`id, deptName, deptCategory（父分类名冗余）, parentId（NULL=父分类节点，V1.9）, deptIntro`
- `DoctorRespDto`：`id, doctorName, title（中文职称）, skill, deptId, deptName, registerFee（挂号费：后端按职称映射计算返回，确认页费用展示来源，见 6.5-⑥）`
- `QuotaRespDto`（V1.9 新增，GET /quotas）：`scheduleId, doctorId, doctorName, title, skill, timeSegment, leftQuota, registerFee`（按科室+日期查医生号源；P4A/P4B 页医生卡数据源）
- `QuotaDayRespDto`（V1.9 新增，GET /quotas/days）：`date, dayStatus（可约/已满/休）`（预约页一周日期条三态）
- `ScheduleRespDto`：`id, doctorId, doctorName, title（V1.9 新增三字段）, workDate, timeSegment（"上午"/"下午"）, totalQuota, leftQuota`（leftQuota 为 0 时前端标"已满"置灰）

### 4.6 医院资源域 · 管理端接口（his-hospital，/admin 前缀，需 ADMIN）

| 故事 | 接口 | 请求（初拟） | 响应（初拟） |
| --- | --- | --- | --- |
| S11 科室分页 | `GET /admin/departments` | `pageNum, pageSize`【待对齐：是否加名称模糊查询】 | `Result<PageDTO<DepartmentRespDto>>` |
| S11 新增科室 | `POST /admin/departments` | `DepartmentSaveReqDto{deptName, parentId, deptIntro}`（3 个字段，边界【待对齐】；V1.9 改传父分类 id，deptCategory 由后端按 parentId 冗余落库） | `Result<Void>` |
| S11 修改科室 | `PUT /admin/departments/{id}` | 同上 body | `Result<Void>`（成功后 evictByPrefix 清科室缓存） |
| S11 删除科室 | `DELETE /admin/departments/{id}` | 路径参数（逻辑删除 delmark=1；V1.9：删除父分类节点时其下仍有子科室 → 400「该分类下存在科室，无法删除」） | `Result<Void>` |
| S12 医生分页 | `GET /admin/doctors` | `pageNum, pageSize, deptId(可选)`【待对齐】 | `Result<PageDTO<DoctorRespDto>>` |
| S12 新增医生 | `POST /admin/doctors` | `DoctorSaveReqDto{doctorName, deptId, title, skill}`（4 > 3，必须封装；title 直传中文职称；doctorName 建议不重名——展示与选择友好，V2.1 起取消回补不依赖姓名，不强制拦截） | `Result<Void>`（deptId 必须存在，校验失败 4001 科室不存在） |
| S12 修改医生 | `PUT /admin/doctors/{id}` | 同上 body | `Result<Void>` |
| S12 删除医生 | `DELETE /admin/doctors/{id}` | 路径参数（逻辑删除） | `Result<Void>` |

#### 4.6.1 统计大屏接口组（his-hospital · /admin/stats/**，需 ADMIN；V1.10 新增，待对齐 #13 定稿：按卡拆分）

> 统计口径（#15 定稿）：趋势/排行/占比 = 近 7 天（含当天）**有效**挂号单（appointment_record，status≠已取消）；KPI 环比「较昨日」、注册用户「较上周同期」；`days`/`limit` 参数本期固定不放开。聚合读 appointment_record / dept / doctor / patient_user（实训单体库直接聚合 SQL）；实现排期 **B·D04**，消费排期 C·D04（mock 先行）/ C·D05（切真实）。DTO 全量字段见 db/dto-contract.md V1.4「统计域」，JSON 示例见 db/mock-data.md V2.4。

| 卡片 | 接口 | 请求 | 响应 |
| --- | --- | --- | --- |
| KPI 四卡+时段分布 | `GET /admin/stats/summary` | 无参 | `Result<StatsSummaryRespDto>`（今日挂号/取消/注册用户/收入 + 四个环比 + 上午/下午单数） |
| 近 7 天挂号趋势 | `GET /admin/stats/trend` | `days=7`（固定） | `Result<List<StatsTrendRespDto>>`（date 升序，近 7 天含当天） |
| 科室挂号量 | `GET /admin/stats/dept-rank` | 无参 | `Result<List<StatsDeptRankRespDto>>`（TOP4 降序，父分类节点不进榜） |
| 医生挂号量 TOP5 | `GET /admin/stats/doctor-top` | `limit=5`（固定） | `Result<List<StatsDoctorTopRespDto>>`（TOP5 降序） |
| 职称挂号占比 | `GET /admin/stats/title-ratio` | 无参 | `Result<List<StatsTitleRatioRespDto>>`（title 中文直传 + count + percent 0–100） |
| 最新挂号动态 | `GET /admin/stats/latest` | `limit=4`（固定） | `Result<List<StatsLatestRespDto>>`（createTime 倒序，仅有效单，名称/费用取下单快照） |

#### 4.6.2 患者管理接口组（his-user · /admin/users/**，需 ADMIN；V1.11 新增）

> 患者账号列表 + 封禁/解封（风控闭环演示点：封禁后患者端登录 → 1003）。DDL 依赖：patient_user 新增 `status` 列（init.sql V2.4，铁律一已对齐）。实现排期 **B·D05**，消费 C·D06（mock 先行）/ D07 起切真实。

| 故事 | 接口 | 请求 | 响应 | 错误码 |
| --- | --- | --- | --- | --- |
| M7 患者分页 | `GET /admin/users` | `pageNum, pageSize, keyword(可选，username 模糊)` | `Result<PageDTO<AdminUserRespDto>>`（id/username/registerTime/status/memberCount 联查计数） | — |
| M7 封禁 | `POST /admin/users/{id}/ban` | 路径参数 | `Result<Void>`（status→已封禁） | 400 用户不存在 |
| M7 解封 | `POST /admin/users/{id}/unban` | 路径参数 | `Result<Void>`（status→正常） | 400 用户不存在 |

#### 4.6.3 挂号单管理接口（his-registration · /admin/registrations，需 ADMIN；V1.11 新增，只读）

> 管理端挂号单**只读**分页列表（不做代取消——取消链路与号源回补事务保留在患者端 S10，避免跨域写风险）。实现排期 **A·D05**，消费 C·D07（mock 先行）。

| 故事 | 接口 | 请求 | 响应 | 错误码 |
| --- | --- | --- | --- | --- |
| M8 挂号单分页 | `GET /admin/registrations` | `pageNum, pageSize, date(可选 yyyy-MM-dd), status(可选 待就诊/已取消), keyword(可选，患者用户名/订单号模糊)` | `Result<PageDTO<AdminRegistrationRespDto>>`（快照字段+联查 username/memberName，默认 createTime 倒序） | — |

#### 4.6.4 排班管理接口组（his-hospital · /admin/schedules/**，需 ADMIN；V1.11 新增；「排班管理」自本期由范围外转正）

> 管理员手工排班维护（基础 CRUD；**Excel 批量导入为扩展可选**，有余力再做）。约束：仅**当天及以后**可新增、仅**未来日期**可改/删（5002）；医生+日期+时段唯一（5001，即 uk_doctor_date_segment）；删除前校验无**有效挂号**（待就诊，5003）；改号源数不得小于已挂号数（5004）。新增落库 left_quota=total_quota。实现排期 **B·D05**，消费 C·D08（mock 先行）。

| 故事 | 接口 | 请求 | 响应 | 错误码 |
| --- | --- | --- | --- | --- |
| M9 排班列表 | `GET /admin/schedules` | `doctorId(可选), startDate/endDate(可选, yyyy-MM-dd)` | `Result<List<AdminScheduleRespDto>>`（含医生/科室联查字段，按 workDate+timeSegment 排序） | — |
| M9 新增排班 | `POST /admin/schedules` | `AdminScheduleSaveReqDto{doctorId, workDate, timeSegment, totalQuota}` | `Result<Void>` | 4001 医生/科室校验失败；5001 排班冲突；400 过去日期/参数校验 |
| M9 修改排班 | `PUT /admin/schedules/{id}` | `AdminScheduleUpdateReqDto{totalQuota}`（仅放开号源数） | `Result<Void>` | 5002 非未来日期；5004 小于已挂号数 |
| M9 删除排班 | `DELETE /admin/schedules/{id}` | 路径参数（逻辑删除 delmark=1） | `Result<Void>` | 5002 非未来日期；5003 已有有效挂号 |

### 4.7 挂号域接口（his-registration）

| 故事 | 接口 | 请求（初拟） | 响应（初拟） | 错误码 |
| --- | --- | --- | --- | --- |
| S8 确认挂号 | `POST /registrations` | `RegistrationCreateReqDto{scheduleId, memberId}`【待对齐：是否冗余传日期/时段做一致性核对】 | `Result<RegistrationCreateRespDto{orderNo, registerFee}>`（≥ 2 字段，必须封装） | 3001 已约满；3002 重复挂号；3003 操作频繁；3005 排班不存在 |
| S9 我的挂号记录 | `GET /registrations/my` | 无参（Token 取 userId），按创建时间倒序 | `Result<List<RegistrationRespDto>>` | — |
| S10 取消挂号 | `POST /registrations/{orderNo}/cancel` | 路径参数【待对齐：路径用 orderNo 还是 id；动词 POST/PUT】 | `Result<Void>`（事务：status='已取消' + left_quota+1；按订单内 schedule_id 直达回补，见 6.5-①） | 3004 非待就诊状态；3006 记录不存在 |

```json
// POST /registrations 请求/响应示例
// 请求
{ "scheduleId": 101, "memberId": 3 }
// 响应
{ "code": 200, "message": "挂号成功",
  "data": { "orderNo": "202609081030221234567890", "registerFee": 30.00 } }
```

`RegistrationRespDto`：`orderNo, memberName（联查 patient_member）, deptName（快照）, doctorName（快照）, workDate, timeSegment, registerFee, status（"待就诊"/"已取消"）, createTime`。

### 4.8 待对齐事项汇总（第 2 天接口评审会逐条销项）

| # | 待对齐项 | 影响接口 | 建议负责人 |
| --- | --- | --- | --- |
| 1 | 注册/登录等 ≤ 3 字段接口是否统一封装 ReqDto | /auth/* | 后端 B（初拟：统一封装，便于扩展） |
| 2 | 分页参数与 PageDTO 字段结构 | /admin/* | 后端 B + 前端 C |
| 3 | Authorization 是否带 `Bearer ` 前缀 | 全部需登录接口 | 后端 A + 前端 D（初拟：带） |
| 4 | ~~科室列表是否支持分类筛选（S4 首页形态）~~ **已定（V1.9）**：GET /departments 返回父子两级平铺（parentId 组树），category 筛选参数废弃；首页不再展示科室，科室选择移入挂号流程 P4 | GET /departments | 前端 D |
| 5 | ~~医生职称存储与传输形式~~ **已定（V1.4）**：`title` VARCHAR 中文直存（主任医师/副主任医师/主治医师），接口直传中文、前端零映射；排序由后端 Service 权重排序（主任→副主任→主治，见 6.4） | 表 doctor + /doctors | 后端 B |
| 6 | ~~密码加密方案~~ **已定（V1.1）**：BCrypt（Hutool `BCrypt.hashpw/checkpw`），顺带解决 1.4 基线 | 表 patient_user | 后端 A |
| 7 | 取消挂号路径参数（orderNo vs id）与动词 | /registrations | 后端 A + 前端 D（初拟：orderNo + POST） |
| 8 | 挂号请求是否冗余传日期/时段做核对 | POST /registrations | 后端 A（初拟：不传，以排班为准） |
| 9 | 业务错误的 HTTP 状态码策略（恒 200 vs 401/500 同步） | 全部 | 后端 A + 前端 C/D（初拟：业务错恒 200 看 code，仅未登录返 401） |
| 10 | 删除/编辑就诊人是否纳入本期 | /members | 全员（需求未明确，初拟不做） |
| 11 | Token 退出失效（黑名单）是否实现 | /auth/logout | 后端 A（初拟：不做，客户端清 Token） |
| 12 | 管理端登录鉴权口径（patient_user 无 role 字段） | POST /auth/login 或新增 /admin/auth/login | 后端 A + 前端 C（初拟：复用 /auth/login + 种子预置管理员账号 admin，不加角色字段；**待销项**，阻塞 M5 登录页真实联调，见管理端设计稿 V1.2 记录） |
| 13 | ~~统计接口组形态（聚合 vs 按卡拆分）~~ **已定（V1.10）**：按卡拆分 6 接口 `GET /admin/stats/*`，见 4.6.1 | /admin/stats/* | 后端 B + 前端 C |
| 14 | ~~大屏图表库选型~~ **已定（V1.10）**：ECharts 5 + vue-echarts（组长 2026-09-10 评审通过，1.4 基线新增依赖获批） | M6 全页 | 前端 C |
| 15 | ~~统计数据时间窗口口径~~ **已定（V1.10）**：趋势/排行固定「近 7 天（含当天）」、KPI 环比「较昨日」、注册用户「较上周同期」；`days`/`limit` 参数本期不放开 | /admin/stats/* | 后端 B |

### 4.9 站内信与消息通知接口组（his-message · 后端 A 全链路；V1.12 新增）

> 数据表 `site_message`（迁移脚本 `db/migrations/V2.5__create_site_message.sql`）。触发链路：①挂号成功/取消 → his-registration 事务提交后经 MQ（topic `his_notification`，见 7.3）异步投递 → his-message 消费者落库；②管理端群发系统公告 → 同步直写（按用户逐行落库，不走 MQ）。实现排期 **A·D08**；消费排期 D·D07（消息中心 P11 mock）/ D·D08（切真实）、C·D09（M10 公告页）。DTO 见 dto-contract V1.6「消息域」，示例见 mock-data V2.6 §5。

| 故事 | 接口 | 请求 | 响应 | 错误码 |
| --- | --- | --- | --- | --- |
| P11 消息分页 | `GET /messages` | `pageNum, pageSize, isRead(可选)` | `Result<PageDTO<MessageRespDto>>`（createTime 倒序） | — |
| P3 铃铛未读数 | `GET /messages/unread-count` | 无参（Token 取 userId） | `Result<Integer>`（单字段直返） | — |
| P11 标记已读 | `POST /messages/{id}/read` | 路径参数（写入 read_time） | `Result<Void>` | 4004 消息不存在或不属于当前用户 |
| P11 全部已读 | `POST /messages/read-all` | 无 body | `Result<Void>` | — |
| M10 群发公告 | `POST /admin/messages` | `AdminMessageSendReqDto{title, content}`（需 ADMIN，全员逐行落库） | `Result<Void>` | — |

**MQ 链路（内部契约，7.3 定稿）**：producer=his-registration（挂号成功/取消事务提交后 `MessageSender.send("his_notification", NotificationMessage)`）；consumer=his-message `NotificationConsumer`（按 type 生成标题/正文落 `site_message`，type：挂号成功/挂号已取消）；消息体 NotificationMessage{type, orderNo, userId, memberName, doctorName, workDate, timeSegment}；消费失败重试 + 日志告警（异常兜底见 A·D08）。

---

## 5. 统一响应体与全局异常

### 5.1 统一响应体（his-common）

```java
public class Result<T> implements Serializable {
    private Integer code;      // 200 成功；其余见错误码表
    private String  message;   // 提示语，可直接 Toast
    private T       data;      // 业务数据，失败时为 null

    public static <T> Result<T> ok(T data)                 { /* code=200 */ }
    public static <T> Result<T> fail(ErrorCode ec)         { /* code+默认message */ }
    public static <T> Result<T> fail(Integer code, String msg) { }
}

/** 管理端分页响应（响应字段 ≥ 2，必须封装） */
public class PageDTO<T> {
    private Long total;        // 总条数
    private Integer pageNum;
    private Integer pageSize;
    private List<T> records;   // 当前页数据
}
```

前端约定：`code === 200` 视为成功取 `data`；否则 Toast `message`；HTTP 401 统一跳登录页。

### 5.2 错误码表（his-common · ErrorCode 枚举）

| code | 含义 | 触发场景 |
| --- | --- | --- |
| 200 | 成功 | — |
| 400 | 参数校验失败 | @Validated 不通过 |
| 401 | 未登录 / Token 失效 | 拦截器校验失败（HTTP 状态同步 401） |
| 500 | 系统异常 | 兜底 |
| 1001 | 用户名已存在 | S1 |
| 1002 | 用户名或密码错误 | S2 |
| 2001 | 就诊人已达上限（3 人） | S7 |
| 2002 | 身份证格式错误 | S7 |
| 3001 | 该时段号源已约满 | S8 |
| 3002 | 重复挂号（同人+医生+日期+时段） | S8 |
| 3003 | 操作频繁，请稍后再试 | S8 限流 |
| 3004 | 当前状态不可取消 | S10 |
| 3005 | 排班不存在 | S8 |
| 3006 | 就诊人不存在或不属于当前用户 | S8 |
| 4001 | 科室不存在 | S12 |
| 1003 | 账号已封禁 | S2（V1.11：封禁账号登录拦截，管理端 M7 封禁触发） |
| 5001 | 该医生当日该时段已有排班 | M9（V1.11） |
| 5002 | 仅未来日期的排班可修改或删除 | M9（V1.11） |
| 5003 | 该排班已有挂号，不可删除 | M9（V1.11） |
| 5004 | 号源数不能小于已挂号数 | M9（V1.11） |
| 4004 | 消息不存在或不属于当前用户 | 站内信（V1.12） |

### 5.3 全局异常处理器（his-common）

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(BizException.class)                    // 业务异常：code + message
    public Result<Void> handleBiz(BizException e) { return Result.fail(e.getCode(), e.getMessage()); }

    @ExceptionHandler(MethodArgumentNotValidException.class) // 参数校验：400 + 首条提示
    public Result<Void> handleValid(MethodArgumentNotValidException e) { ... }

    @ExceptionHandler(NotLoginException.class)               // 未登录/Token 过期：HTTP 401
    public Result<Void> handleNotLogin(NotLoginException e) { ... }

    @ExceptionHandler(Exception.class)                        // 兜底：500 + error 日志
    public Result<Void> handleOther(Exception e) { ... }
}
```

**全组规约**：业务校验失败一律 `throw new BizException(ErrorCode.XXX)`；禁止用返回 null / false 隐式表达失败；Controller 不写 try-catch。

---

## 6. 数据库设计（MySQL 8.0）

### 6.1 设计约定

1. 每表必有公共四字段：`id`（BIGINT 自增主键）、**`delmark`**（0-正常 / 1-已删除，**六表齐备**，V2.2）、`create_time`、`update_time`；"不删除"的业务限制由后端接口层实现（订单不提供删除接口，见 6.5-⑤），不由表结构缺字段承担；
2. `create_time` / `update_time` 由数据库默认值维护（DEFAULT / ON UPDATE CURRENT_TIMESTAMP），代码零侵入（patient_user 的 register_time 同理由库默认值维护）；
3. 表名（V2.0 定稿，无统一前缀）：`patient_user / dept / doctor / schedule / patient_member / appointment_record`；库名 `neusoft_hospital`（utf8mb4_unicode_ci）；表和字段注释必填（实训硬性要求）；
4. 逻辑删除：`delmark=1` 即删除，查询自动过滤（MyBatis-Flex 实体字段标注 `@Column(isLogicDelete = true)`，详见 6.4）；
5. 快照冗余：挂号记录冗余下单时刻的**科室名/医生名**（dept_name_snap / doctor_name_snap），不冗余就诊人姓名——展示时联查 patient_member（S9 要求历史记录不受后台改动影响）。

### 6.2 表关系总览

```
patient_user 1 ──── n patient_member（就诊人，归属患者）
dept 自关联 ──── parent_id（V1.9 父子两级：父分类 1 ──── n 子科室，父分类七类预置）
dept 1 ──── n doctor（医生，必填外键 dept_id）
doctor 1 ──── n schedule（排班：医生×日期×时段×号源；唯一键 uk_doctor_date_segment，见 6.5-③）
schedule 1 ──── n appointment_record（挂号记录：doctor_id / schedule_id 关联 + 名称快照，见 6.5-①）
```

### 6.3 建表脚本（权威版本：`db/init.sql` V2.4 = cloud_hospital.sql 定稿 + 安全审查修复 + delmark 补齐 + 父子分类/排班种子调整 + patient_user 状态列）

> **完整 DDL 与种子数据以 `db/init.sql` 为唯一权威版本**（本节不再内嵌全文，避免两处版本漂移——任何修改走铁律一并同步）。**迁移规则（V1.11 定，组长 2026-09-10 决策）**：自 V2.4 起 init.sql 定格为全量基线、**不再直接修改**；后续一切表/字段增删改（DDL）一律由 B 通过 `db/migrations/V{版本}__{描述}.sql` 独立增量脚本交付（含头部注释与回滚语句，已提交脚本禁改禁删），全新环境按「init.sql → migrations 升序执行」初始化；约定详见 `db/migrations/README.md`。六表速览：

| 表 | 关键字段 | 约束 / 索引 | 支撑故事 |
| --- | --- | --- | --- |
| patient_user | username（唯一）、password（BCrypt 密文）、register_time | uk_username；delmark | S1 S2 S3 |
| dept | dept_name、dept_category（父分类名冗余）、parent_id（父分类 id，NULL=父节点，V1.9）、dept_intro | idx_parent_id；delmark | S4 S11 |
| doctor | dept_id（必填外键）、doctor_name、title（中文职称）、skill | idx_dept_id；**CHECK title 三档枚举（V2.1）**；delmark | S5 S12 |
| schedule | doctor_id、work_date、time_segment（上午/下午）、total_quota、left_quota | **uk_doctor_date_segment 唯一键（V2.1，6.5-③）**；CHECK 时段枚举 + 号源非负且 ≤ 总量（V2.1）；delmark（V2.2 补齐） | S6 |
| patient_member | patient_user_id、member_name、gender（中文）、id_card、phone（必填）、birthday | idx_patient_user_id；**CHECK gender 枚举（V2.1）**；delmark | S7 |
| appointment_record | order_no（唯一）、patient_user_id、member_id、**doctor_id、schedule_id（V2.1 新增）**、dept_name_snap、doctor_name_snap、work_date、time_segment、register_fee、status（中文） | uk_order_no、idx_patient_user_id、idx_schedule_id（V2.1）；**CHECK status 枚举 + 费用非负（V2.1）**；delmark（V2.2 补齐；**后端不提供删除接口**，6.5-⑤） | S8 S9 S10 |
| site_message（V2.5 迁移脚本新增·第七表） | user_id、title、content、msg_type（挂号成功/挂号已取消/系统通知）、order_no（可空）、is_read、read_time | idx_user_read、idx_user_create；CHECK msg_type 枚举；delmark 预留 | 站内信（V1.12，4.9） |

> 种子数据（与 mock-data.md 一一对应）：**7 父分类 + 4 子科室**（V1.9 父子两级，子科室 id 1–4 不变）、8 医生（职称三档齐、建议姓名不重复——展示友好非强制）、每医生**当天~+7 天**排班（`CURDATE()+N` 动态生成，重置库后当天与未来 7 天永远有数据；每医生每天至多一条，满足唯一键；含休息日与 充足/紧张/已满 差异化余号，见 init.sql V2.3 排班段注释）；用户/就诊人/挂号记录走业务流程产生。

### 6.4 实体映射与 MyBatis-Flex 约定（1.8.8）

- **BaseEntity 六表统一（V1.8 恢复）**：全部实体统一继承 `BaseEntity{id, delmark, createTime, updateTime}`；`delmark` 标 `@Column(isLogicDelete = true)`，Flex 默认 0 正常/1 已删除与 DDL 一致：delete 自动改写 `UPDATE ... SET delmark='1'`，查询自动追加 `delmark='0'`（V2.2 起 schedule / appointment_record 同样适用）；`id` 统一标 `@Id(keyType = KeyType.Auto)`；`createTime/updateTime/registerTime` 保持 null 不写值，数据库默认值生效，不配 InsertListener/UpdateListener 双轨；
- **分页为 Flex 内置能力，无需任何插件配置**：`mapper.paginate(pageNum, pageSize, queryWrapper)` 返回 Flex 的 `Page<T>`，Service 层统一转成 5.1 节 `PageDTO`；
- **医生按职称排序（title 为中文，无天然大小）**：数据量小（每科室个位数医生），推荐 **Service 层内存权重排序**（Map 定义 主任医师→1 / 副主任医师→2 / 主治医师→3，升序排），简单可靠零坑；备选 SQL `ORDER BY CASE title ...`（了解即可，答辩可讲两种方案取舍）；
- **排班扣减号源必须用条件更新保证原子性**（禁止"先查再改"），Flex 用 `UpdateChain + setRaw`：

```java
// 等价 SQL：UPDATE schedule SET left_quota = left_quota - 1
//          WHERE id = ? AND left_quota > 0
boolean deducted = UpdateChain.of(Schedule.class)
        .setRaw(Schedule::getLeftQuota, "left_quota - 1")
        .where(Schedule::getId).eq(scheduleId)
        .and(Schedule::getLeftQuota).gt(0)
        .update();
if (!deducted) throw new BizException(ErrorCode.SCHEDULE_FULL);   // 3001 已约满
```

- **取消挂号回补**（6.5-① 口径，V2.1 简化）：按 orderNo 查订单 → 以订单内 `schedule_id` **直达定位排班** → **同一事务**（`@Transactional`）内 `status='已取消'` + `left_quota + 1`（`setRaw(..., "left_quota + 1")`）。医生改名/同名均不影响回补（V2.0 的"姓名快照反查"方案已废弃）；
- 查询优先 `QueryWrapper`（Lambda 写法），复杂 SQL 才写 XML（放各模块 `resources/mapper/`，默认扫描路径已覆盖）；
- 可选增强：引入 `com.mybatis-flex:mybatis-flex-processor`（编译期注解处理器，与 Lombok 共存）生成表字段常量类，可用 `DEPT.DEPT_NAME.like(...)` 风格防字段名手滑。

### 6.5 数据库设计取舍与实现约定（V2.2 口径，全组统一执行，答辩可讲）

| # | 设计点 | 统一口径 |
| --- | --- | --- |
| ① | 挂号记录**同时存 id 关联（doctor_id / schedule_id）与名称快照**（V2.1 修复） | id 用于业务：取消挂号按 `schedule_id` **直达回补**号源，医生改名不断链；名称快照用于历史展示（S9 记录不受后台改名影响）；就诊人姓名不冗余，展示时联查 patient_member |
| ② | schedule 携带 delmark（V2.2 补齐）；过期排班不主动删除 | 过期数据由"近 7 天"查询时间窗自然过滤（查询同时自动过滤 delmark=0）；**删除医生时在同一事务内将其未来排班逻辑删除（delmark=1）**，防孤儿排班（S12 删除接口实现；V2.2 起不再物理删除，全库删除口径统一） |
| ③ | schedule 的 (doctor_id, work_date, time_segment) 为**唯一键**（V2.1 修复） | 数据库层杜绝重复排班（重复排班会导致挂号条件更新各扣各的 → 超卖/回补错行）；造数/写入仍建议"先查后插"以获得友好报错，唯一键是最后防线 |
| ④ | 中文枚举 + CHECK 约束（V2.1 新增） | title / time_segment / gender / status 带 CHECK 枚举（MySQL 8.0.16+ 强制生效），left_quota 带 ≥0 且 ≤total_quota 约束兜底超卖；应用层校验（400 错误码）仍是第一道防线，CHECK 是数据库层最后防线 |
| ⑤ | appointment_record 携带 delmark（V2.2 补齐），但**后端不提供删除接口** | "订单永久保留（审计需要）"是**代码层的限制**（Controller 不暴露删除端点），不是表结构限制——逻辑删除字段是防数据丢失的统一兜底，六表齐备（答辩考点：为什么限制放代码层而不是省字段） |
| ⑥ | 挂号费来源：doctor 表无费用字段，定价模型=**按职称统一映射** | 真理源为 his-common 常量（主任医师 30 / 副主任医师 20 / 主治医师 15）；**对外展示**：`GET /doctors` 返回 `DoctorRespDto.registerFee`（后端按 title 计算带出）——确认挂号页费用展示的唯一来源，前端禁止自建价格映射；**下单**：请求体不含费用，后端重算并落库为快照 register_fee（防篡改；此后改价不影响历史订单）；改价只改常量并全组周知（答辩考点：为什么费用不信任前端） |
| ⑦ | patient_user 的 register_time 与 create_time 并存 | 语义分工：注册时间（业务口径，S1 汇报用）与记录创建时间（审计口径）；均由库默认值维护，代码不写值 |
| ⑧ | username 唯一键 × 逻辑删除的组合隐患 | 本期无用户删除功能，安全；若未来做注销，须先改写 username（如 `{username}_del_{id}`）再置 delmark=1（逻辑删除与唯一索引冲突是答辩高频考点） |
| ⑨ | 排班管理写约束（V1.11）：仅未来可改/删、唯一键冲突、有效挂号保护 | 新增排班仅限**当天及以后**、改/删仅限**未来日期**（5002）；`uk_doctor_date_segment` 唯一键冲突 → 5001；删除前校验无「待就诊」订单（5003），改 totalQuota 不得小于已挂号数 total-left（5004）；删除=逻辑删除 delmark=1（与 6.5-⑧同理，不做物理删）；封禁用 status 列而非 delmark（delmark 语义保留为「注销/删除」，封禁可逆，二者不混用） |

---

## 7. 中间件落地方案

### 7.1 Redis key 设计（与需求文档一致，集中在 `RedisKeyConstants`）

| 用途 | key 模式 | TTL | 说明 |
| --- | --- | --- | --- |
| 科室列表缓存（S4） | `dept:list:{category}:{delmark}` | 1800s（加随机抖动防雪崩） | Cache-Aside：命中直返、未命中查库回填；只缓存 List 不缓存 Page |
| 防重复挂号锁（S8） | `reg:lock:{patientId}:{doctorId}:{yyyyMMdd}:{segment}`（segment=上午/下午） | 30s | SETNX 抢锁失败即拒；`finally` 中释放 |
| 挂号限流计数（S8） | `reg:limit:{patientId}:{yyyyMMddHHmm}` | 60s | INCR 后 >5 拒绝（key 精确到"患者+分钟"，与需求文档示例格式差异已按分钟口径修正） |

### 7.2 接入步骤（第 4 天，半天）

1. `his-api` 打开 pom 中 `spring-boot-starter-data-redis` 依赖与 yml 配置段，`his.middleware.redis.enabled: true`；
2. 在 `his-common/port/impl/redis` 新增三个 Redis 实现类（Cache / SetnxLock / CounterRateLimit），业务代码**零改动**；
3. 验收：①科室二次查询命中缓存（日志可见未回源）；②Apifox/脚本并发提交同一"患者+医生+日期+时段"，仅 1 单成功；③第 6 次挂号请求返回 3003。

### 7.3 MQ 接入（V1.12 转正定稿：第 8 天扩展专题纳入本期，A 全链路）

- **场景（定稿）**：①挂号成功通知；②挂号已取消通知——两场景统一走 topic **`his_notification`**（V1.12 起替代原 `his_registration_success`），消息体 NotificationMessage（type/orderNo/userId/memberName/doctorName/workDate/timeSegment，见 4.9 / dto-contract V1.6）；③管理端群发公告为同步直写，**不经 MQ**；
- **落地**：`MessageSender` port 双实现切换（2.4.1）——`LoggingMessageSender`（降级/联调观测，`his.mq.enabled=false` 默认）↔ `RocketMQMessageSender`（rocketmq-spring-boot-starter **2.3.1**，适配 Spring Boot 3.x，1.4 基线定稿）；producer 位于 his-registration（事务提交后发送，`TransactionSynchronization.afterCommit` 或事务外发送），consumer 为 his-message 模块 `NotificationConsumer`（`@RocketMQMessageListener`，落 site_message）；
- **本地环境（Docker）**：部署 NameServer(9876) + Broker(10911)，命令与 `his.mq.enabled` 开关说明归档 A·D08；拔掉 MQ 主流程不中断（降级为日志，站内信缺失可接受——答辩讲"异步最终一致 + 降级可观测"）；
- **演示链路**：挂号 → 消息中心未读 +1（MQ 控制台/日志可观测）→ 取消挂号 → 取消通知。

### 7.4 缓存三大问题防护（实训要求的工程素养点，答辩加分）

| 问题 | 防护手段 |
| --- | --- |
| 缓存穿透 | 科室查询无此分类时缓存空值（短 TTL 60s） |
| 缓存雪崩 | TTL 加随机抖动（1800s ± 300s） |
| 缓存击穿 | 热 key 互斥重建（锁内查库回填，可作加分演示项） |

---

## 8. 工程规范与协作

### 8.1 代码规约要点

- 包/类名英文、有意义的注释；Controller 只做校验和编排，业务写在 Service；
- 统一走 `Result` + `BizException`，禁止各写各的返回格式；
- Entity 不出网，一律转 RespDto；敏感字段（密码）任何接口不得返回；
- 提交前本地自测通过（启动 + Apifox 用例），禁止提交不可编译代码。

### 8.2 Git 规范（云效 Codeup / Gitee）

- 分支：`master`（受保护，只接受 MR）← `dev`（日常集成）← `feature/xxx`（按故事拉分支，如 `feature/S8-registration`）；
- commit 前缀：`feat:` / `fix:` / `optimize:`（与需求文档 5.7 一致），每日必有提交；
- 冲突在 `dev` 上由模块负责人牵头解决；`.gitignore` 排除 `target/`、`node_modules/`、`oh_modules/`。

### 8.3 每日节奏

- 09:00 站会（10 分钟：昨日完成 / 今日计划 / 阻塞）；
- 第 2 天 15:00 **接口评审会**：销第 4.8 节待对齐项，产出本文档 V1.1 + Apifox 定稿；
- 每日 21:00 前提交小组日报（负责人轮值）。

---

## 9. 四人分工与十天计划

> 本章为**概要矩阵**；每人逐天的完整任务清单、验收标准、依赖关系与认领签字表见独立文档**《任务分工说明书》**。

### 9.1 角色与职责矩阵（姓名组内对齐后回填）

| 角色 | 成员 | 负责模块/故事 | 附加职责 |
| --- | --- | --- | --- |
| 组长 · 后端 A | 待定 | his-registration（S8/S9/S10）+ 全部中间件 port 与 Redis 接入 + JWT/拦截器 | 架构与拆分主导、接口评审会主持、答辩架构讲解 |
| 后端 B | 待定 | his-user（S1/S2/S3/S7）+ his-hospital（S4/S5/S6/S11/S12）+ 建库脚本 | Knife4j/Apifox 文档维护、日报汇总 |
| 前端 C | 待定 | Vue3 管理端（S11/S12 全部页面 + 分页/校验） | 管理端与后端联调、测试用例表 |
| 前端 D | 待定 | 鸿蒙患者端（S1–S10 全部页面 + withLogin 守卫 + axios 封装） | App 与后端联调、演示真机准备 |

共同职责：每日提交、参与联调回归、答辩材料各自负责章节。

### 9.2 十天任务矩阵（对应教学日历）

| 天 | 日历主题 | 后端 A（组长） | 后端 B | 前端 C（Vue） | 前端 D（鸿蒙） | 当日验收 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 启动/环境 | 建仓 + 单模块骨架 + Result/异常/枚举 | 环境核对（JDK17/Maven 镜像/MySQL）+ DDL 初稿 | Vite 脚手架 + 路由/布局 | DevEco 工程 + 模拟器跑通 | M1：/doc.html 可访问，hello 接口通 |
| 2 | 数据库/登录注册 | S1/S2 接口 + JWT 签发 + 登录拦截器 | 六表 DDL 评审定稿 + S4/S5 查询接口 | 科室管理页（Apifox mock） | 登录/注册页 + axios 拦截器封装 | S1/S2 Apifox 通过；**15:00 接口评审会；17:00 完成多模块拆分** |
| 3 | 挂号流程/管理端 | S8 初版（无锁）+ S9/S10（事务） | S6 排班接口 + S11/S12 管理端接口 | 科室/医生管理页联调 | 首页/科室/医生/排班页 | M2：S4–S12 接口 Apifox 全通 |
| 4 | Redis | 分布式锁 + 限流接入 S8 | 科室 Cache-Aside + 管理端写时删缓存 | 管理端联调回归 | 就诊人/确认挂号页联调 | M3：缓存命中、并发拦截、限流生效 |
| 5 | 鸿蒙基础联调 | 接口缺陷修复 + 造数脚本 | Knife4j 文档整理 | 管理端造数 + 回归 | withLogin 守卫 + 全页面联调 | 患者端主流程（模拟器）跑通 |
| 6 | 鸿蒙核心/全流程 | 挂号全流程串联 + 事务复核 | 接口文档交付稿 | 管理端完善（分页/校验/UX） | 挂号成功/记录/取消页 | M4：S4→S10 全流程 App 真联通 |
| 7 | 扩展：AI 导诊（范围外） | Spring AI + SSE 接口（可选） | 协同 + 数据准备 | 可选接入对话页 | 可选接入对话页 | 扩展演示（不计入验收） |
| 8 | 完善 + MQ 扩展 | RocketMQ 接入 MessageSender | 边界 case 与异常兜底完善 | 回归 + 截图素材 | 回归 + 截图素材 | M5：取消事务/清缓存验证；（扩展）MQ 消息可见 |
| 9 | 整合/答辩准备 | JMeter 压测（可选）+ 架构章节 | 接口/数据库文档终稿 | PPT 功能截图 | 演示脚本 + 真机彩排 | 交付物齐备（见需求 6.2） |
| 10 | 模拟面试/答辩 | 架构与高并发讲解 | 数据库/接口讲解 | 管理端演示 | App 全流程演示 | M6：答辩通过 |

### 9.3 里程碑验收清单

| 里程碑 | 时点 | 验收项 |
| --- | --- | --- |
| M1 | 第 1 天末 | 骨架可启动；/doc.html 打开；六表可执行建表成功 |
| M2 | 第 3 天末 | 多模块拆分完成且回归通过；S4–S12 全部接口 Apifox 走通 |
| M3 | 第 4 天末 | 科室二次查询命中缓存；并发重复挂号仅 1 单成功；每分钟第 6 次被限流 |
| M4 | 第 6 天末 | 鸿蒙端挂号全流程真联通（S4→S8 + S9/S10） |
| M5 | 第 8 天末 | 取消挂号号源 +1 恢复；后台改科室前台即时更新；（扩展）MQ 通知 |
| M6 | 第 10 天 | 交付物齐全（PPT/压缩包/过程文档/日报），答辩演示通过 |

---

## 10. 风险与应对

| # | 风险 | 应对 |
| --- | --- | --- |
| 1 | 零基础 + 多模块上手慢 | 单模块起步；包即模块边界；拆分做成机械清单由组长带做；拆分当晚全量回归 |
| 2 | Redis/机房环境故障 | port 接口 + Noop 降级实现，`enabled:false` 即可继续演示核心流程 |
| 3 | 联调窗口紧（第 5–6 天） | 接口先行 + Apifox mock，前端不等后端；每日回归 |
| 4 | 范围蔓延（AI 导诊/MQ） | 第 7 章与需求文档第 7 章一致标记"范围外"，验收不含，进度让位主线（V1.11 注：排班管理/统计大屏/管理端运营页已由组长决策转正纳入本期；AI 导诊/MQ 仍为范围外扩展） |
| 5 | 关键人单点（组长掌握架构） | 第 3 天起每个域至少两人熟悉（结对 + 每日代码走读 15 分钟） |
| 6 | 并发缺陷（超卖/重复单） | 号源扣减用条件更新 + 分布式锁双保险；第 9 天 JMeter 压测验证 |

---

## 附录 A · 追溯矩阵（故事 → 域 → 模块 → 接口 → 表 → 负责人）

| 故事 | 功能 | 领域 | 模块 | 主要接口 | 表 | 负责人 |
| --- | --- | --- | --- | --- | --- | --- |
| S1 | 用户注册 | 用户与认证 | his-user | POST /auth/register | patient_user | B |
| S2 | 用户登录 | 用户与认证 | his-user | POST /auth/login | patient_user | B |
| S3 | 退出登录 | 用户与认证 | his-user | POST /auth/logout | — | B / D |
| S4 | 科室列表 | 医院资源 | his-hospital | GET /departments（父子两级平铺，+Redis 缓存） | dept | B（缓存 A） |
| S5 | 按科室看医生 | 医院资源 | his-hospital | GET /doctors | doctor | B |
| S6 | 医生排班 | 医院资源 | his-hospital | GET /schedules（doctorId / deptId 二选一） | schedule | B |
| S7 | 添加就诊人 | 用户与认证 | his-user | POST /members、GET /members | patient_member | B / D |
| S8 | 确认挂号 | 挂号 | his-registration | POST /registrations（锁+限流） | appointment_record、schedule | A |
| S9 | 挂号记录 | 挂号 | his-registration | GET /registrations/my | appointment_record（联查 patient_member） | A / D |
| S10 | 取消挂号 | 挂号 | his-registration | POST /registrations/{orderNo}/cancel | appointment_record、schedule | A / D |
| S11 | 科室管理 | 医院资源 | his-hospital | /admin/departments CRUD（写时删缓存） | dept | B / C |
| S12 | 医生管理 | 医院资源 | his-hospital | /admin/doctors CRUD | doctor | B / C |
| 迭代·V1.9 | 当天/预约挂号号源 + 日期条 | 医院资源 | his-hospital | GET /quotas、GET /quotas/days | schedule（联查 doctor） | B / D |
| 迭代·V1.10 | 统计大屏（管理端） | 医院资源（聚合读） | his-hospital | GET /admin/stats/* 六接口 | appointment_record / dept / doctor / patient_user | B / C |
| 迭代·V1.11 | 患者管理 + 封禁 | 用户与认证 | his-user | GET /admin/users、POST /admin/users/{id}/ban、/unban | patient_user（V2.4 加 status 列，联查 patient_member 计数） | B / C |
| 迭代·V1.11 | 挂号单管理（只读） | 挂号 | his-registration | GET /admin/registrations | appointment_record（联查 patient_user / patient_member） | A / C |
| 迭代·V1.11 | 排班管理（CRUD，原范围外转正） | 医院资源 | his-hospital | GET/POST /admin/schedules、PUT/DELETE /admin/schedules/{id} | schedule | B / C |
| 迭代·V1.12 | 站内信（MQ 异步通知 + 群发公告，原范围外转正） | 消息域 | his-message（A） | GET /messages、/unread-count、POST /{id}/read、/read-all、POST /admin/messages；MQ topic his_notification | site_message（迁移脚本 V2.5，联查 patient_user） | A / C / D |

## 附录 B · 全局公共约定速查

- 统一前缀 `http://localhost:8080/api`；管理端加 `/admin`；文档 `/doc.html`
- 响应一律 `Result<T>{code, message, data}`；成功 code=200；业务错恒 HTTP 200，未登录 HTTP 401
- 请求 > 3 字段 → `XxxReqDto`；响应 ≥ 2 字段 → `XxxRespDto`；Entity 不出网
- 业务失败统一 `throw new BizException(ErrorCode.XXX)`，由全局异常处理器兜底
- 每表必有 `id / delmark / create_time / update_time`（六表齐备，V2.2）；订单不提供删除接口（后端代码层限制，6.5-⑤）
- 技术栈以 1.4 节基线为准（JDK 17 + Spring Boot 3.1.5 + MyBatis-Flex 1.8.8 boot3 starter），版本不得私自升降
- 中间件一律走 `common/port` 四接口；`his.middleware.redis.enabled` 一键切换
